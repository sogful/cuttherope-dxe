name: Actions Dispatch Probe

on:
    push:
        branches: [main]
    workflow_dispatch:

permissions:
    contents: read

jobs:
    probe:
        runs-on: ubuntu-latest
        steps:
            - name: Confirm workflow dispatch
              run: |
                  echo "event=${{ github.event_name }}"
                  echo "sha=${{ github.sha }}"
