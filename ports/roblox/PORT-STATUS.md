# Port working snapshot - 2026-09-18

## Progression, rendering performance and UI batch

Normal progression now uses the original 17 classic-box star thresholds and sequential per-level unlocks. Zero-star wins and Skip unlock the next level. Settings has an Unlock all levels checkbox: enabling it clones real progress, completing/skipping/resetting affects only that disposable copy, and disabling it restores real progress. The cheat is off by default and is never exported. Cosmetic/audio/language preferences are shared, not reset by disabling the cheat.

ProgressData, SessionState, SaveClient, ProgressStore and ProgressServer implement a versioned server profile. Published servers use CutTheRopeDxProfilesV1 with an expiring owner lease, validated commands, five-second debounced saves, 45-second renewal and leave/shutdown release. Failed loads never create a replacement save; failed writes retain dirty state. Studio deliberately uses an isolated memory adapter and never touches production DataStores. Save failures are exposed in settings. Published-server persistence remains unverified.

All five ProgressChecks groups passed in Studio: source progression/thresholds, disposable cheat copy, save/reload and competing leases, failed load/write retry, and expired-owner rejection. Tests were disabled after running. The main client is enabled. No production save was reset and no place was published.

RopeDrawing reuses Bernstein coefficients for the same degree-N Bezier and exact existing sample positions. The current cache weakly retains unused lengths; active curves retain their own coefficients. RopeRasterizer clears old/new dirty tiles and uploads their bounding region instead of the full 1024-pixel-wide surface. RopeRenderer skips identical geometry/style/camera frames. PackSelection avoids redrawing settled boxes and skips offscreen card updates. Resolution, 2x supersampling, rope colors and 60 Hz physics are unchanged.

Before/after Studio CPU samples (median presentation ms, 30 samples after 10 warmup frames): 1-1 2.317/0.882; 1-7 5.608/3.776; 7-25 4.590/2.776; 11-25 3.574/1.845; 16-25 6.704/3.630; 17-25 2.977/1.411. These are bounded CPU measurements, not published-client FPS claims. Initial optimized Bezier comparison passed with maximum deviation 0.002322 design pixels. Every output pixel and simulated partial-upload composite matched the original rasterizer, including clearing vanished ropes. Baseline modules are retained under ports/roblox/tests/baselines. Final cache-lifetime/numerical-stability follow-up still needs its Studio rerun.

Fallback fonts now center their source font-height metrics in the existing GoodDog line grid; atlas pixels and outlines are unchanged. tools/roblox/prepare_locale_fonts.py --emit derives these offsets from the shipped font files. DragScroll adds mouse dragging and inertia to credits/skin lists, keeps native wheel/touch behavior, and suppresses item activation after dragging. Credits auto-scroll stops on a drag. The result transition is constructed fully initialized while disabled and below the game, warmed there, reset to its open pose, then raised; no initially closed/default-position overlay is ever deliberately exposed.

Next after the last level returns to box selection and opens the source GameFinished popup, with localized title/body, original MenuPopup quad 0 and the source 0-to-1.1-to-.9-to-1 animation. The blue paper-only atlas was visually inspected and imported as rbxassetid://125393897695949. Its localhost upload helper was stopped. Studio-only DxStudioResult allows this navigation to be tested when the unlock cheat is enabled; it cannot alter normal progress or run in published clients.

Validation limitation: after the successful progress/render tests, Studio's Client/Server bridges and screenshot capture became unreachable, then the instance disconnected. All 536 Luau files parsed before the final small cache/test-hook follow-up. tools/roblox/check_rope_math.py subsequently checked the current cache/recurrence against the old implementation through 128 control points using a float32 Vector2 shim, maximum difference 0.002086 design pixels; this is not an engine/GPU test. The main implementation was synced before disconnection; latest RopeDrawing cache changes, PackSelection settled-state signature and DxStudioResult hook require resync. Mouse-drag feel, localized screenshots, final-popup rendering and frame-by-frame result handoff still require live UI validation. Detailed evidence is in ports/roblox/generated/review/progress-render-validation.json. Do not describe these as visually verified yet.

## Imported audio live and silent disclaimer startup

The user imported all 155 WAVs into Uploads. Exact Audio inventory names and owner 3059456361 were verified against manifest.json, then tools/roblox/import_audio_receipts.py generated the complete AudioAssets mapping. game.CreatorId confirms the same place owner; universe is 6982172202. receipts.json/assets.json retain the inventory evidence. All 155 assets load in Studio with zero missing or failed entries; all 155 TimeLength values exactly match the source manifest. This is Studio validation, not a published-server or acoustic listening test.

Live integration found two previously untestable issues. Bare audio-ID PreloadAsync reported Failure even when the Sound itself had loaded; typed Sound preloads succeed. The loader now retains six silent music anchors plus the primed effect voices and ignores duplicate preload calls. Options rebuilt their page before a separate Activated sound callback executed, suppressing the click. Main-client controls now dispatch their sound and action in one ordered callback, retaining deduplication and the original pre-toggle sound preference.

Twelve audio test groups still pass, including 68 paired maps with unchanged physics. Live menu music has advancing TimePosition and nonzero PlaybackLoudness. Actual options and pause sound/music clicks have the expected event counts and mute state. Pause holds music at exactly the same timestamp; re-enabling it while paused starts a new track, matching DX. Captured real Sound.Played events include taps, menu/game music, rope cut, three distinct star cues, mouth opening, chewing and win. Detailed evidence: ports/roblox/generated/review/audio-import-validation.json. Earlier audio-validation.json remains the pre-import baseline.

The user requested a silent, disclaimer-only white startup screen. Removed the partial logo, zepto_logo_bubbles call and delayed Loading textures text. Attribution uses the existing black, zero-stroke SmallFont GoodDog atlas, with corrected multiline spacing so all three lines remain visible near the bottom. StartupPresentationChecks verifies the missing decorations, zero startup effect events and the correct atlas; a captured clone of the actual ready startup page was screenshot-inspected. The diagnostic is disabled for normal Play.

## Audio wiring and remaining-feature audit

AudioController, AudioCatalog, AudioAssets and LevelAudio are installed in Studio. Source registrations come from Resources.cs, MusicPack.cs, CTRSoundMgr, OmNomSoundResolver and om_nom_skins.json, not inferred filenames. tools/roblox/prepare_audio.py stages 155 unchanged validated WAVs (6 music, 149 classic/skin effects, 98,309,264 bytes) in ports/roblox/generated/audio/import. manifest.json contains resource identifiers, exact importer display names, file hashes, duration/sample format and skin mappings. tools/roblox/import_audio_receipts.py validates exact-name/owner/type receipts before generating AudioAssets. The initial implementation used an empty mapping while waiting for user import; that blocker is now resolved by the verified 155-asset mapping described above. See ports/roblox/generated/audio/README.md.

Playback has separate music/effect groups, overlapping pooled voices, four-worker preload, missing-asset diagnostics and late-one-shot suppression. Music is one looping menu track or the original five gameplay tracks with the source's capped repeat avoidance. Music continues across restarts and result screens and switches on entering/leaving gameplay. Source SoundMgr.Pause/SuspendSoundEffects operate on loops, not existing one-shots: short sounds ring out, new muted effects are suppressed, music and electric loops pause/resume in place. Re-enabling music inside pause starts it immediately, matching the original dedicated test. Both options and pause buttons are connected. Button handlers are deduplicated when result controls are reparented. Active-runtime binding excludes staged/hidden levels and standalone physics diagnostics.

Gameplay audio sites cover tension-selected rope cuts, automatic-hook attachment/buzz, bubble capture/pop, split merge, star 1/2/3, mouth opening/closing/chewing/sad, skin opt-in greetings/excitement, night snores/star relighting, candy breaks, pumps, spider activation/fall/theft, hats, wheels, gravity flips, rotating spikes/electric loops, discs, ghost morphs, steam valve states, lantern capture/release, mouse spawn/grab/player-drop, conveyor movement/wrap/handoff and win/splash/menu cues. Fast pointer wheel subdivision emits only one sound per real pointer movement. Physics events are emitted inside fixed ticks and use an independent audio RNG.

Validation: 27 existing C# audio/resource-scanner tests pass. The recording-backend Studio checks cover catalog/skin declarations, pause/music toggle semantics, loop mute ownership, missing/late effects, bounded overlap/reuse, gameplay event sites and three-star chewing/unbind, plus identical candy trajectories with and without audio over 68 maps from all 17 boxes. Actual UI clicks verified options and pause sound/music flags, P pause/resume and a three-star 1-1 swipe/result with the win cue. This is event/state validation, not audible or moderation validation. The full existing regression launcher passes: all 425-map construction/rendering checks, viewport/timing/presentation groups, exact Cardboard goldens, 744 other reference routes and 21 mover routes (765 total). All 524 Luau files parse. Final focused audio results and regression evidence are in ports/roblox/generated/review/audio-validation.json.

tools/roblox/audit_remaining_port.py inventories all layers of all 425 classic maps. No unrecognized classic gameplay object tags remain. Tutorial nodes occur in 35 maps and still lack their overlays/director behavior. SessionState still has only in-memory scores, preferences and selected skins; persistent saves remain unimplemented. All-box/all-level unlocking is intentional. Legacy hidden02/hidden03/hiddenElement/spikesSwitch tags are also undispatched by DX's current object loader, not newly missing port mechanics. The editor was deliberately removed; intro video remains optional/unported; other DX campaigns are outside the 17-classic-box scope. Existing Pillow additive-glow approximation and tiny randomized pre-bind star bob remain documented fidelity gaps. Startup is intentionally disclaimer-only at the user's request; seasonal Christmas music/event variants and independent app-focus audio suspension are not wired. Game/Roblox-menu pause is wired. See ports/roblox/generated/review/remaining-port-audit.json. No commit or publish; no external helper process was started for this batch.

## Mechanical Box: all 425 classic maps enabled

Mechanical (17), the last classic box, now has all 25 authored maps. Its 48 conveyors (38 manual, 10 automatic) follow LoadConveyorBelts, ConveyorBelt, ConveyorBeltObject and the original scene update/input order. Supported carried objects are stars, bubbles, hooks, bouncers, hats, pumps and steam tubes. The port preserves rotated binding regions, manual dragging/coasting, centerline correction, object separation, end scaling/alignment, multiple wraps per movement, activation-priority handoff and automatic-belt ownership. Wrapping an authored hook cuts only sibling ropes on the same candy; wrapping an automatic hook translates its intact rope without cutting siblings. Both current and previous body positions/pins move together. Moving/rail/ghost hooks are excluded from binding. Collected stars and captured bubbles leave the belt immediately. Outcomes cancel manual drags while automatic belts and world physics continue.

ConveyorVisuals follows BuildVisuals/CreatePiece using raw, un-restored ObjConveyor quads 0..6, source mirrored corners/rails, native 63-unit plate tiles, direction arrows and end highlights. Partial end tiles scale as in ConveyorBeltVisual.Draw. Tiles are pooled and retain immutable image crops. Gameplay draw order places belts after discs and before bubbles, with manual activation ordering and automatic belts last. Bound visuals scale with their objects; SteamTube's parent/body/valve scale inheritance is preserved. PackConfig supplies bgr_17_p1 and CharSupports 16. The generator verifies 29 lossless crops/mirrors; the reviewed atlas/background receipts are in ports/roblox/generated/mechanical-atlas/assets.json. No character skins were uploaded. Both assets join startup residency (304/304 loaded, zero unavailable or failed textures).

tools/roblox/run_mechanical_reference.ps1 exports 75 C# routes and 30,284 every-tick samples. All 75 pass: candy and previous positions are exact; maximum carried-object error is 0.0001220703125 world pixels, with zero topology, bubble, hook-cut or conveyor-owner mismatches. The harness aligns the recorded random +/-3-unit initial star bob before replay, because Star.WillBind stops that randomized idle timeline. The port keeps authored star starting coordinates; reproducing that tiny randomized pre-bind bob is not claimed. The comparison caught ordinary MathF trig versus the collision lookup table, split-half retirement and cancellation of a held belt on loss. Ten original conveyor/movement/rope-isolation unit tests pass.

Nine focused Mechanical groups pass: single/multiple wraps, rotated drag/inertia, manual/automatic ownership, split-candy rope isolation, automatic-hook wrapping, star/bubble removal, all-map pooled rendering, restart immutability and continued automatic motion after loss. All ten general-box groups pass across 425 maps, as do seven viewport groups/eight shapes and five fixed-simulation groups. The timing sweep now includes 21 representative levels, including Mechanical 17-1/16/24/25 at 4/10/60/144 visual FPS; the 4..240 FPS clock/stall checks remain passing. All 669 prior Fabric-through-Pillow reference routes and 21 separate mover routes pass. Cardboard trajectories remain exact and all ten presentation groups pass. All 519 Luau files parse.

Live evidence so far: 17-1 actual mouse dragging moved its bubble and an actual swipe cut the right rope; 17-16 automatic belts repeatedly wrapped stars/hats/bouncers, and an actual drag moved/wrapped its steam tube. 17-25's mixed rail/hazard/hat/steam/vertical-belt layout was screenshot-inspected. These are representative interactions and bounded reference routes, not complete walkthroughs of every Mechanical puzzle. Evidence is recorded in ports/roblox/generated/review/mechanical-box-validation.json and mechanical-reference.json. Temporary asset HTTP server PID 21980 was stopped after uploading the two reviewed images. No commit or publish.

All classic boxes are now implemented; tutorials and persistent saves remain unfinished. The later audio sections above supersede the original missing-audio status. The source map loader intentionally remains scoped to the 17 classic boxes, not every object in other DX campaigns or an editor.

Final handoff: actual pause -> Mechanical level grid -> carousel -> Space -> 17-1 button navigation passed. All 43 diagnostic LocalScripts are disabled and the main client is enabled. Fresh normal Play is on Mechanical 17-1, playing, unpaused, not debug-frozen or transitioning, without an avatar. All 304 resident textures are ready and the console is empty. The sequential regression launcher uses the existing Dx-prefixed diagnostic names; an initial test-only missing-script wait was corrected before the full passing run.

## Pillow Box and result/bubble/mouse corrections: 400 maps

Pillow Box (16) is enabled with all 25 original maps, 25 light bulbs, 76 ropes, 42 bubbles, 20 hats and 18 steam tubes. The loader follows LoadLightBulbs/NormalRopeLoad, including named bulb bindings and the original authored light radius. Light bulbs are independent, non-edible physics bodies: gravity, ropes, automatic hooks, bubbles, pumps, steam, bouncers and hats work for them independently of the candy. NightMechanics follows LightProximity, GameScene.CandyCollision and the sleep/wake/lights-out paths. Only lit stars can be collected; bulbs cannot collect stars or feed Om Nom. Strict radius boundaries, hidden-in-hat lighting exclusion, sleeping/waking and loss of the final light are covered by focused tests. Bulb physics continues after a win.

Source comparisons exposed a shared rope-lifetime detail: invisible cut tails retain their constraints until rope disposal. Removing those constraints prematurely, especially during a bulb's hat journey, caused large divergence in 16-25. The port now preserves inert-tail constraints, and Pillow's hat release uses the source deferred detachment. All 594 prior Fabric-through-Cheese routes pass after that change; Cardboard goldens remain exact.

Presentation uses traced ObjLighter quads 0..42, ObjStarNight 0..30, CharAnimationsSleeping 0..6, FxSleep 0 and CharSupports 15. Night stars use their 300-square source canvas, not the normal star's 552-square canvas. Fireflies, lighting crossfades, the sleep/wake effect, original breathing and ZZZ timelines are ported; XML skins use their existing Sleeping definitions. The lossless crop generator verifies 83 crops. Three reviewed atlases and bgr_16_p1 are recorded in ports/roblox/generated/pillow-atlas/assets.json; no skin uploads. Roblox GUI has no additive blend mode: PillowLightRaster uses a sampled-background approximation with bounded alpha (.38) to brighten the backdrop without hiding foreground ropes/Om Nom. This 96-square editable glow is a visual approximation, not a pixel-exact additive pass. All 302 resident textures loaded, with zero unavailable assets or failures.

Requested fixes: result closing panels now prewarm below gameplay, close over the still-enabled level, build/warm the result page while hidden, then switch surfaces under fully closed panels. The old menu/loading page is never enabled during the closing animation. A live three-star win recorded nine transition frames under test load, zero invalid/stale-menu frames, and the result page ready before the surface switch. Normal and ghost bubble capture squash now plays once and settles on circular ObjBubble quad 7; quad 16 is still stretched and is not held. This deliberately overrides the source BubbleAnimationFactory's perpetual 4..16 loop to match the requested behavior. Mouse capture/handoff immediately cancels residual rope-spin while preserving the candy's facing; its physical path and ownership remain unchanged.

Validation: tools/roblox/run_pillow_reference.ps1 exports 75 original C# routes and 22,854 every-frame samples including tick zero. All 75 pass; 72 position trajectories are exact, the maximum deviation is 0.393372 world pixels (below .5), with zero bubble/presence/transport mismatches. Lighting eligibility is tested separately, not claimed as a trace comparison of already-collected stars. All 31 original lighting/removal/presentation/sleep/collision tests pass. Eight focused Pillow groups pass. All ten general-box groups and seven viewport groups pass across 400 maps/eight shapes. Five fixed-simulation groups pass, now including 17 representative maps at 4/10/60/144 FPS, the 4..240 FPS clock sweep, and a five-second stall safely capped at 15 ordinary steps. Ten presentation and eight Cheese groups also pass. All 435 Luau files parse successfully.

Live checks: 16-1 actual swipes lowered the bulb, lit all three stars, woke Om Nom and completed a three-star win; result -> level grid -> Pillow carousel -> the actual 16-25 grid button works. In 16-25, a swipe released the bulb through a hat and into an automatic hook while the candy remained in the steam jet. 16-9 was inspected with separate bulb/candy bubbles. These are representative live checks and bounded reference routes, not complete walkthroughs of every Pillow puzzle. Evidence: ports/roblox/generated/review/pillow-box-validation.json and pillow-reference.json. New modules include NightMechanics, NightVisuals, PillowFrames, PillowLightData, PillowLightRaster and LevelPack16, plus the Pillow golden/focused checks and source exporter.

Final recheck: normal and ghost bubbles both settle on quad 7 in the focused test. Live 16-9 shows circular bubbles; clicking the candy bubble pops only that bubble and leaves the bulb's bubble intact. A diagnostic assertion initially inspected the unused ghost pool rather than its active item; the corrected test passes. All changed Studio scripts were synced with full readback verification. Heavy concurrent diagnostics briefly made MCP client requests unreachable; sequential checks and Stop/Play restored access without a Studio restart.

Handoff: all 40 diagnostic LocalScripts disabled, main client enabled. Fresh normal Play is on Pillow 16-1, playing, unpaused, not debug-frozen or transitioning, no avatar, all 302 textures ready, and an empty console. Temporary asset server PID 13608 was stopped. Next box is Mechanical (17); tutorials, game audio and persistent saves remain unfinished. No commit or publish.

## Cheese Box and render-independent simulation: 375 maps

Cheese Box (15) is enabled with all 25 original maps and 61 authored mice. MouseMechanics follows LoadMice, Mouse, MiceObject, the scene input/update order and per-candy ownership. It preserves strict grab/click radii, active durations, logical-index cycling, rotated entry/exit candy paths, single occupancy, retained bubbles, deferred rope detachment, click-to-drop, handoff, capture-device removal and outcome shutdown. Split halves and lantern-held candy cannot be grabbed. Source registration has a subtle shared-child-table behavior: when a fallback mouse is registered before index 1, the same sprite container remains in both groups' child lists. Its initial animation advances twice per scene tick. A handoff to a later-authored mouse can also update its entry/path in that same tick. Both behaviors are preserved; smoothing them away broke source comparisons.

MouseVisuals uses restored ObjMouse quads from the exact animation registrations (entry, idle, eyes, carry, exit), original rotated bounce positions, a single shared body/eye pair and ordered hole/body passes. Candy stays above the mouse and other objects. PackConfig provides bgr_15_p1 and CharSupports quad 14. The generator verifies 30 lossless crops. The reviewed mouse/support atlas and background are recorded in ports/roblox/generated/cheese-atlas/assets.json. No character skins were uploaded. Body/eye animation uses prewarmed immutable crops, with no frame churn. HUD stars now also use immutable, prewarmed animation crops instead of changing the UV rectangle of one ImageLabel. The original partially transparent art/opacity is retained.

SimulationClock schedules 60 fixed ticks per second on Heartbeat; RenderStepped only presents gameplay and advances visual UI. LevelRuntime.advance/step/present separate gameplay from drawing, with update retained as a combined test interface. Star pickup/expiry, mouth checks, hazard/outcome handling, elapsed time and gameplay effects now run per fixed tick, not once after all ticks in a rendered frame. Rope-relaxation/rotation bookkeeping no longer depends on draw calls. Win/loss delay starts on the actual outcome tick. The C# physics delta remains its deliberate float .016, preserving existing trajectories. A client that is completely stalled cannot execute Lua: catch-up is capped at 15 steps (.25 seconds), and excess wall time is counted in StallTimeDropped. This slows game time under extreme lag rather than enlarging a physics step or skipping collisions/timers. It is not a promise of background execution while Roblox is hung, server authority, or continuous collision detection for arbitrary velocities.

Validation: tools/roblox/run_cheese_reference.ps1 exports 75 real C# routes and 27,542 every-frame samples including tick zero. All routes pass; 73 trajectories are exact, the other two (15-2 cut routes) differ by at most 0.071333 world pixels, below the existing .5-pixel tolerance. Mouse state, ownership, input and body counts have zero mismatches; timer error is below 0.00000006 seconds. All 17 original mouse ownership/grab/shutdown tests pass. Eight focused Cheese groups pass, including source registration, strict boundaries, retained bubbles/deferred ropes, same-payload handoffs, eligibility, shutdown, immutable restart and pooled rendering.

Five SimulationChecks groups pass: clock rates 4/5/10/20/30/60/120/144/240 FPS; identical three-star/win behavior at 4/5/10/20/60/144 FPS; rendering without gameplay advancement; identical bodies, rotations, outcomes and timers across 14 representative maps at 4/10/60/144 FPS; and a five-second stall matching exactly 15 ordinary steps with 4.75 seconds discarded. All 519 prior Fabric-through-Lantern differential routes pass, and Cardboard goldens remain exact. All ten general-box groups and seven viewport groups pass across 375 maps/eight shapes. All ten presentation groups pass; the burst-lifecycle test now asserts bounded stall time before checking expiration. The final small cleanup moved rope-rotation bookkeeping wholly into DxPhysics; Cheese and timing checks were rerun afterwards.

Live checks: 15-1 three-star win using an actual swipe, automatic lower-to-upper mouse handoff and pointer release into Om Nom; 15-11 five-hole/90-degree carried-candy scene; 15-19 mixed rope/pump/gravity scene; pause -> level grid -> Cheese carousel -> 15-25 actual grid button, with hats/steam/spikes/mice screenshot-checked. Cardboard 1-1 was also swipe-completed after the timing/HUD change. These are bounded comparison routes and representative live checks, not full puzzle walkthroughs of every Cheese level. All 298 resident textures loaded with zero failures/unavailable assets.

Evidence: ports/roblox/generated/review/cheese-box-validation.json and cheese-reference.json. New code/data: MouseMechanics, MouseVisuals, CheeseFrames, SimulationClock, LevelPack15; CheeseChecks, SimulationChecks, CheeseGoldenRunner/Checks and 75 fixture modules; prepare_cheese_assets.py, prepare_cheese_traces.py, run_cheese_reference.ps1 and RobloxCheeseTraceTests.cs. All 97 changed/new Studio sources were verified by full readback. Length-checked JSON source reads caught and retried one truncated fixture before it reached Studio. The initial unyielded trace run hit Studio's script timeout; the trace runner now yields every 120 ticks and each route, and the timing diagnostic yields through long renderer loops. The temporary asset HTTP server PID 15836 was stopped. No commit or publish. Next box is Pillow (16); game audio, tutorials and persistent saves remain unfinished.

Handoff: all 38 diagnostic LocalScripts disabled, main client enabled. Fresh normal Play confirmed Cheese 15-1, playing, unpaused, not debug-frozen, no avatar, all 298 textures loaded and an empty console. Studio remains open for the user. Completion sound played.

## Lantern Box: 350 maps

Lantern Box (14), all 25 authored XML maps, 68 lanterns and five moving lanterns, is enabled. Source traces: Lantern.cs, LoadLanterns, GameScene.Update/Input/Draw, PendingLanternCapture, LanternRelease, NormalRopeLoad and the shared rope-release path. LanternMechanics keeps scene callbacks separate from each object's dispatcher: immediate ownership/gravity suppression, delayed capture after .05 seconds, touch release after .01, candy reveal after .1, and recapture cooldown .5. Every active lantern displays the shared candy; authored update order pins its physics point to the last lantern until release. A tapped moving lantern transfers its previous-frame displacement, not an invented launch impulse. Initially captured maps and the loader's shared-point reset order are preserved, and fixed ropes are not attached to pre-captured candy. Split halves cannot enter. Held candy cannot enter hats, hit spikes or open Om Nom's mouth; other interactions retain their source eligibility.

Capturing candy pops its bubble and cuts its ropes. Differential testing exposed an important short rope-detachment delay: the source retains constraints until the cut fade drops below 1.95, rather than removing the tail immediately. Lantern capture now uses that deferred path; already-cut tail fragments are hidden without erasing their remaining physics. Other boxes retain their existing release behavior. This correction eliminated the 14-7 divergence and the smaller release errors in 14-1/2/15.

LanternVisuals uses restored ObjLantern fire/active/idle quads 0/1/2, source activation/deactivation, inner-candy squash/appear/hide/idle, randomized glow timing and path rotation. Candy variants 0..2 use ObjLantern quads 3..5; all 49 remaining candy variants use quad 10 of their exact CandySkinHelper resource. PackConfig selects support quad 13 and bgr_14_p1. The generator verifies all 56 crops byte-for-byte and records source registrations. Three reviewed uploads are in ports/roblox/generated/lantern-atlas/assets.json. No character skins were uploaded. Each lantern has its own ordered child passes inside a source-ordered lantern group. A screenshot caught the earlier equal-Z shell/candy batching ambiguity; the explicit hierarchy fixes it. All frames are immutable/staged and there is no GUI allocation during animation or release cycles. Normal preload is 296/296, with zero failed/unavailable assets.

tools/roblox/run_lantern_reference.ps1 exports 75 real C# scenes (three routes per map), 19,956 every-frame samples including the initial state. After the deferred-rope correction, all 75 routes match exactly for candy position, previous position, lantern position/angle, ownership, state and accepted input; every measured maximum is zero. The routes are bounded traces, not full walkthroughs of all 25 puzzles. Thirteen existing C# lantern tests pass. Ten focused Luau groups pass, covering capture/reveal/cooldown, strict touch bounds, moving inertia, rope/bubble cleanup, removal callbacks, pre-captured maps, interaction gates, all candy frames, restart/resize, draw order and no frame churn. All 444 previous Fabric-through-Steam differential routes pass; Cardboard goldens remain exact. Ten presentation, ten general-box and seven viewport groups pass, including construction/rendering/simulation of all 350 maps and camera/input projection at eight viewport shapes.

Live pointer checks: 14-1 three-star win (swipe plus two lantern taps); 14-8 moving-lantern release/reveal with inherited motion; pause to level selection, back to the box carousel, select Lantern, then the actual 14-25 grid button. Six captured lanterns, rotating/moving hats and selected-pack art were screenshot-checked. All three moving-scene inner candies remain visible with the ordered hierarchy. An apparent input failure was actually an automatic failure/restart occurring before the slower follow-up tool query; freezing immediately after a quarter-second of simulation confirmed a real release. Studio-only input telemetry now records world coordinates, handling and lantern ownership, and resets on level restart so counters from different runs are not confused.

Evidence: ports/roblox/generated/review/lantern-box-validation.json and lantern-reference.json. Implementation: LanternMechanics, LanternVisuals, LanternFrames, LevelPack14; generators prepare_lantern_assets.py, prepare_lantern_traces.py; tests LanternChecks, LanternGoldenChecks, LanternGoldenRunner and 75 fixture modules. All 93 changed/new Studio sources matched local readback, with the final input-telemetry/reset edits separately rechecked and the focused tests rerun. The active Studio reconnected as d77854a5-32da-4d40-91db-b67212ce53ee after the user restarted it. Its recovered Steam module matched local source. No commit or publish.

Tooling caution: raw cmd.exe type output silently lost chunks in nine reads this turn, including the main client and LevelPack14. Play caught the syntax errors. Every file was re-read through a single-line JSON envelope containing [IO.File]::ReadAllText plus its length, checked before parsing, re-synced where necessary, and compared against full Studio readback. Use that envelope for future syncs, not unchecked terminal output. Official Luau CLI 0.738 was downloaded into tools/roblox/luau-cli, its published SHA-256 verified, and used for local parsing checks. No custom offline Roblox-vector mock was introduced. Temporary asset HTTP server PID 23460 is stopped. Next box is Cheese (15); audio, tutorials and persistent saves remain unfinished.

Handoff: all 35 diagnostic LocalScripts are disabled, main client enabled. A fresh normal Play has an empty console, 296/296 loaded textures, no avatar, and confirmed Lantern 14-1 in the playing state, unpaused and not debug-frozen. Studio remains connected and running this scene for the user. Completion sound played.

## Steam Box: 325 maps

Steam Box (13), all 25 original XML maps and 69 steam tubes, is enabled. SteamMechanics follows SteamTube.cs, LoadSteamTubes and GameScene.OperateSteamTube: three pressures (98.7/282/423 world units plus the original sine modulation), rotated valve reach of 120, source overlap bounds, velocity damping/centering, gravity-dependent direction/divisors, positional impulses and exponential falloff. Forces run after pumps and before hats/hazards/bouncers/bubble lift. Every surviving split half is affected independently; retired/transported bodies are excluded. Valve state changes immediately, but an already-playing half-turn is not restarted. Tube state belongs to the runtime, not the authored map.

SteamVisuals uses ObjPipe quads 0..1 without restored trim, and restored animation ranges 2..12, 13..23 and 24..34. Pipe/valve/back puffs draw after hats but before ghosts/hooks/candy; front puffs draw after candy/bubbles but before spiders/effects. It retains 7/14/20 staggered puffs, 0.6-second eased travel, 1-to-1.5 growth, per-variant offsets and pressure-dependent/random heights. Old pressure puffs finish their current pass. Forty-two prewarmed slots per tube hold immutable crops, with no per-frame Instance creation; extreme repeated taps recycle the oldest retiring puffs. Visual random phases are not C# RNG-identical. The orange background and sitting platform come from PackConfig bgr_13_p1 and CharSupports 12. Both reviewed assets are registered in ports/roblox/generated/steam-atlas/assets.json; no skin uploads.

Source comparison: tools/roblox/run_steam_reference.ps1 builds the real C# code and exports 75 routes (25 untouched, 25 pressure/cut sequences, 25 staggered valves/cuts/pops), up to 480 ticks. The harness stops at candy removal or source scene replacement, excluding automatic retries. All 75 pass: 71 candy trajectories are exact; worst difference is 0.3080603 world pixels in the long automatic-rope 13-9 route. The explicit tolerance is half a 1440-high world pixel. Height error is below 0.000016 and valve rotation error below 0.000031 degrees, with zero state/input/body-count/bubble-ownership mismatches. An initial float-vs-double idle timer error in valve animation was caught and corrected. The original C# SteamTubeValveTests also pass (2/2).

Seven focused Steam groups pass: authored maps/count, scaled/rotated input, uninterrupted valve turns, force bounds/centering/gravity alignment, split-body lifecycle, immutable restart and pooled art/layers/resize. All 75 Spooky and 50 DJ differential routes and all 244 earlier Fabric-through-Buzz routes pass. Cardboard goldens remain exact. Ten Spooky groups, eight disc groups and ten presentation groups pass. Seven viewport groups pass across all 325 maps at eight shapes, including added Steam runtime resize scenes. The previous run's pending same-render bubble swap and all-Spooky-form renderer checks are now verified. All ten general-box groups pass, including 180-tick simulation and construction/rendering of all 325 maps and explicit Steam visual counts. Its unsupported-pack assertion was corrected to the supported(pack,index) signature and rerun successfully. Evidence: ports/roblox/generated/review/steam-box-validation.json.

Live Studio: actual mouse valve cycling 0 -> 1 -> 2 -> 0 without cutting the rope; 13-1 swipe-cut and low-pressure catch collecting two stars; high vertical and horizontal steam producing a three-star win. Six-pipe/split-candy 13-22 was screenshot-checked and its sideways valve moved the left half. Normal pause -> level select -> 13-16 grid-button navigation passed, with split/bubble/rope rendering inspected. All 293 resident textures loaded, zero failures/unavailable assets; avatar remains absent. These checks are not full walkthroughs of all Steam puzzles.

Working files: SteamMechanics, SteamVisuals, SteamFrames, LevelPack13, the updated importer and shared runtime/draw layers, tools/roblox/prepare_steam_assets.py, tools/roblox/prepare_steam_traces.py, and ports/roblox/tests/SteamChecks.client.luau / SteamGoldenChecks.client.luau. All 31 changed/new Studio sources matched local readback, including the final diagnostic-only assertion correction. All 33 diagnostic LocalScripts were disabled and the main client enabled before the final normal Play restart. That fresh Play had an empty console and 293/293 loaded textures, and accepted the Steam 13-1 load request. The final handoff-state query then returned 'Place is not open'; list_roblox_studios retained the same ID but its name was null. The final playable scene therefore could not be confirmed. All implementation/testing had completed before this disconnect. Check the newly connected place before resuming; local files are authoritative if Studio recovery lost unsaved edits. Temporary asset server PID 13732 is stopped; completion sound played. No commit or publish. Next box is Lantern (14); tutorials, game audio and persistent saves remain unfinished.

## Continuous disc label and Spooky Box: 300 maps

The yellow DJ disc center now uses one continuous image, assembled losslessly from ObjVinil quad 2 and its mirror. The transparent straight-edge column is excluded and the halves overlap by two source pixels inside the baked image. The generator asserts an opaque join. A single transformed ImageLabel replaces the independently rotated halves; the original center pin and minimum sticker scale remain. Live Studio rotation to 42.105 degrees showed no split seam. Assets and provenance: tools/roblox/prepare_ghost_assets.py and ports/roblox/generated/ghost-atlas.

Spooky Box (12), all 25 original XML maps and 53 ghosts, is enabled. GhostMechanics follows Ghost.cs, LoadGhosts, GhostForm and the GameScene ownership/update paths. Allowed forms cycle Bubble -> Grab -> Bouncer, skipping unavailable forms; idle is restored by bubble release or rope cutting. The source touch radius is 80, incoming fade 0.36 seconds and outgoing fade 0.16 seconds. Automatic hooks use the normal capture path; radius -1 selects the nearest surviving split half or the whole candy. Outgoing ropes detach, fade and unregister; outgoing bouncers remain collidable until their source retirement phase. Retired arrays preserve creation order, which matters to iterative rope constraints. Bubble slots are reused while capture order remains creation-ordered.

Captured ghost bubbles lock their ghost, follow the candy and release on pop, replacement, hat entry or candy removal. When split halves merge, ghost bubbles outrank plain bubbles; a second ghost bubble stays parked until the survivor is popped. Ordinary bubble art is also hidden if capture and replacement both occur between rendered frames. GhostVisuals uses the registered ObjGhost 0..6 crops, exact cloud offsets/scales/cadences, independent body/face bobbing, supporting clouds, bouncer animation, hook radius and a prewarmed seven-particle morph pool per ghost. Forms are staged during level loading and pooled through morphs. Roblox straight-alpha GUI compositing approximates the source's premultiplied ghost draw pass; random visual phases are not deterministic C# RNG matches.

PackConfig selects CharSupports 11 and the single bgr_12_p1 background. This pack has no P2 seam; tall-view background rendering now handles that absence explicitly. Three reviewed uploads: ghost/support atlas, Spooky background and continuous disc sticker. No skin uploads. Texture residency reached 291/291, zero failures/unavailable assets. The temporary localhost asset server was stopped.

Source comparison: tools/roblox/run_ghost_reference.ps1 exports 75 real GameScene routes (25 idle, 25 morph cycles, 25 cycles with cuts/pops), up to 360 ticks. After preserving dynamic rope creation order and mirroring off-screen half retirement in the harness, all pass: 71 candy trajectories exact; maximum error 0.192004 world pixels; zero form, rope topology, ownership or accepted-input mismatches. Fixtures are generated by tools/roblox/prepare_ghost_traces.py. These bounded routes are not complete puzzle walkthroughs.

Live Studio: disc rotation/seam inspection; normal box-strip navigation to Spooky and the 12-2 grid button; idle/bubble/hook/bouncer art; automatic ghost rope capture and swipe-cut return to idle; captured-bubble locking and click-to-pop return to idle; ghost bouncer collision with a registered hit and two stars in 12-1. No full Spooky win is claimed. Next box is Steam (13). Tutorials, game audio, persistent saves and later boxes remain unfinished. No commit or publish.

Validation and handoff: eight focused ghost groups passed (forms/touch bounds, capture lock/pop, nearest split anchor/cut, replacement ownership, parked merge, bounded retirement, staged forms/resize and authored maps). All 50 DJ and 244 earlier Fabric-through-Buzz differential routes passed; Cardboard goldens remained exact. Eight disc, ten presentation, ten general-box and seven viewport groups passed, including construction/rendering of all 300 maps and camera fits at eight shapes. Evidence: ports/roblox/generated/review/ghost-box-validation.json. All 33 changed/new Studio sources matched final local readback; all 31 diagnostic LocalScripts are disabled, main client enabled. Studio is in Edit mode, not left playing or debug-frozen. The final Play restart stalled in the MCP with 'Start play hasn't finished yet' while the state query reported Edit. The small same-render bubble-art cleanup and its new test, plus the extended all-Spooky-form renderer test, are synced but have not run yet. Restart Studio, then run GhostChecks and GhostGoldenChecks to finish that recheck. No helper server remains.

## Avatarless shell and DJ Box: 275 maps

Added CutTheRopeServer in ServerScriptService and CutTheRopeShell in ReplicatedFirst. CharacterAutoLoads is disabled in both the place and server script. Existing/unexpected player characters are removed; computer/touch movement is Scriptable, PlayerModule controls are disabled and Reset Character is disabled. The camera is Scriptable with no subject. An opaque, full-viewport, safe-area-unclipped black ScreenGui is created before the main game's requires, below gameplay/menus and retained through all screen changes. Existing world/cat assets were not removed. A fresh client has no character, jump/death sounds or movement warnings. With the other game GUIs hidden, the captured viewport was entirely black. W/Space did not spawn or move an avatar. The initial harmless Player:Move warning from disabling an active default controller was eliminated by setting Scriptable movement before client initialization.

DJ Box (11), all 25 original XML maps, is now enabled. DiscMechanics ports the C# rotatedCircle loader, size/handle registration, float/table-trig pointer rotation, per-object baseline ownership, pinned rope-anchor updates, rotating pump direction, free-bubble movement, one-handle behavior, click/drag cut protection, and the source's small authored-size square for rotating targets. Self-moving hooks and player rails stay off discs. Captured/consumed bubbles are not rotated; map data is never mutated, so restarting restores all positions. Overlapping discs reorder with the source 0.2-second face fade and stationary temporary copy; enclosing discs stay behind nested ones. Temporary copies are visual-only in this port. Shared-hook/free-bubble membership controls the overlap contours.

DiscVisuals follows RotatedCircle's draw order and integer pivots: fixed face/highlights/center, rotating mirrored sticker halves, independently scaled idle/active handles, antialiased active ring and shared-object intersection contours. Discs render after Om Nom and before bubbles, pumps, hazards, hooks and candy. Overlap-copy art is staged during loading and reused, avoiding new-frame flashes; expired contour holders are reclaimed. DJ background P1/P2 and support quad 10 are registered from PackConfig/CharSupports. Source face quad 0 is 1066px wide: four lossless texture pieces produced a visible filtering seam in Studio, so only this face is resampled to 1020px in one atlas and still drawn at its original 1066px world extent. All other crops/mirrors are lossless. Five active new textures (three atlas pages, two backgrounds), no skin uploads. Retired quadrant asset receipts are retained locally and not referenced by the game. Final residency: 288/288, zero failures/unavailable textures.

Validation: tools/roblox/run_disc_reference.ps1 exports 50 real C# scene routes (25 idle, 25 driven through GameScene pointer input; up to 240 ticks, terminating at candy removal). All pass in Luau: sampled candy, anchor and free-bubble positions match exactly; worst handle/angle error 0.0000610352, pump angle error 0.0000125488; zero topology or bubble-ownership mismatches. Eight new focused checks pass, including the shell, platform exclusions, immutable restarts, one-handle input, target bounds, overlap ordering/pooling and state-preserving resize. General checks construct/render all 275 maps and simulate each for 180 ticks. Seven viewport groups pass, including all 275 maps at eight shapes and DJ multi-disc runtime resize. All 244 prior Fabric-through-Buzz differential routes pass; Cardboard goldens remain exact, and all ten presentation checks pass. These bounded routes do not establish that every complete DJ puzzle is solvable.

Live Studio review: actual pointer quarter-turns in 11-1 moved all three anchors and collected both side stars; no full DJ win is claimed. Pause/level exit and the normal 11-2 level-grid button work. One-handle art, 11-18 shared contours, active highlights and bringing a selected disc forward were inspected. A simulated 320x600 host and pause layout were screenshot-checked, with black backing around the test host. Space toggled gravity in 8-1 and R reset it while the avatar stayed absent. Screenshots caught and corrected the split-face seam. Temporary localhost upload servers were stopped.

Evidence: ports/roblox/generated/review/disc-box-validation.json, disc-reference.json, ports/roblox/tests/DiscChecks.client.luau and DiscGoldenChecks.client.luau. Rebuild fixtures with tools/roblox/run_disc_reference.ps1 and tools/roblox/prepare_disc_traces.py; art with tools/roblox/prepare_disc_assets.py --emit-luau. All 29 changed/new Studio sources match local readback; all 29 diagnostic LocalScripts are disabled for normal handoff. Fresh normal Play has an empty console, 288/288 textures and no avatar. Studio is running DJ 11-1, unpaused and without simulated host sizes. No commit or publish. Spooky Box (12) is next; its ghost mechanics, game audio, tutorials, persistent saves and later boxes remain unfinished.

## Dynamic viewport, responsive screens, higher Level caption

Removed the gameplay, menu and transition 16:9 constraints and the obsolete letterbox bands/border. The dimmed tile remains underneath as a fallback, but normal screens cover the full viewport. `ViewportLayout` follows the current C# ViewportLayout/ContentFit/LayoutMath/FittedContentFit split: logical short side 1440, unrestricted visible aspect, fixed authored/world coordinates, uniform content scale, centered content, edge-anchored chrome and cover-fitted backgrounds. Wide compositions receive a safety fit rather than overflowing portrait widths. No source asset changes/uploads.

GameplayCamera now accepts a visible viewport independently of its authored tracking window. LevelRuntime projects the world uniformly, inverse input mapping uses the same logical surface, and Space resolves the actual visible center. Resizing re-fits a running or paused scene without advancing/resetting physics, score time, camera tracking or level state. A held gesture is ended on resize so points from the previous projection cannot cut across the new one. Level backgrounds and Cosmic Earth overlays cover/reproject the dynamic view. The connected rope raster now allocates an aspect-correct surface (long side at most 1024), atomically replaces it on aspect changes and counteracts the camera using the actual surface extent; it no longer clips to a fixed 1024x576 drawing area.

HUD stars/buttons stay in their corners, the level number stays bottom-left, and only the secondary Level caption is raised by 12 design units. Main/options/language/reset/results/pause/skin compositions scale uniformly; covers, loading tape/cutter and animated box transitions cover the viewport separately. Menu shadows keep rotating. Credits use a responsive scrolling window (source 1300-wide column / 170 logical top-bottom insets), maintain wrapping and GoodDog/fallback fonts under inherited UIScale, and keep the back button separate. The pack strip derives one, two or three visible slots from source PackStripLayout, preserving snap/drag state and the screen-locked Om Nom reveal through resize.

Validation: seven new ViewportChecks groups pass. Camera fit and inverse-coordinate checks cover all 250 maps at eight shapes: 1920x1080, 1600x1000, 1024x768, 900x900, 2560x1080, 3440x1440, 390x844 and 320x800. Representative ordinary, tall, split, wheel, gravity, Toolbox and Buzz scenes retain physics/time/tracking state across resize. Background/closed-panel coverage, uniform world scale, caption anchoring, and real EditableImage rope surface coverage pass. Carousel maximum measured screen-locked-character drift is 0.000122071 pixels. The initially failing hidden-GUI check was sampling Roblox's lazily evaluated AbsoluteSize before its signal/layout cascade; explicitly resolving size and awaiting the layout phases fixes the diagnostic without relaxing the 0.01-pixel assertion.

Regression checks: all 58 Toolbox/Buzz C# differential traces pass; camera golden maximum error 0.001450000003 with intro/30-60Hz/25-map checks passing; ten presentation and seven pack-scroller checks pass. Live mouse/keyboard navigation and screenshots checked native full-window, portrait, 4:3 and ultrawide presentations, pack/level selection, options/languages (including Japanese), credits, skins, pause, a portrait 1-1 win/results screen, and a running/paused 1-15 resize. Paused candy coordinates and elapsed time stayed exactly unchanged. These are representative rendered checks and bounded tests, not a full puzzle walkthrough at every aspect or physical-device testing.

Evidence: `ports/roblox/tests/ViewportChecks.client.luau`, `ports/roblox/generated/review/viewport-validation.json`. All 13 modified/new Studio sources matched local readback. Texture residency reached 283/283, zero unavailable/failures. No commit or publish. All 27 diagnostics are disabled for normal handoff; simulated host sizes were removed by restarting Play. Fresh normal Play > Cardboard > 1-1 navigation passed, with a clean console and both board/host at the full 1122x801 viewport. Studio is running 1-1; completion sound played.

## Fast wheel input, Space, Toolbox and Buzz: 250 maps

Gift wheel input now reconstructs coalesced pointer arcs into at-most-4.5-degree substeps before calling the unchanged source WheelControl drag routine. Roblox can otherwise deliver a fast sweep as a single event, losing nearly the entire turn to DX's per-event clamp. The source's segment rollover, geometric extension limit, three-point floor, exclusion zone and direct-event differential fixtures are preserved. Live four-quarter-turn input retracted 7-1 from 11 to 8 rope points, with measured dial rotation about -349 degrees. The focused coarse-versus-fine gesture check passes; no claim that this solves every Gift star route.

Space calls the gravity switch nearest the visible camera center, measured in world coordinates after zoom/scroll. The nearest authored switch is recorded in LastGravityButton; ties choose the first authored record. No switches means no action. Text entry, processed input, pause, Roblox's menu, transitions, intro touch lock and finished levels remain guarded. A scrolled-camera/multiple-switch test verifies selection, and live Space/P/Space/P/Space toggled, remained unchanged while paused, then toggled back. Box-selection Space/Enter behavior is unchanged.

Toolbox (9) and Buzz Box (10), 25 maps each, are enabled. The importer now walks every XML layer like LoadObjectsFromMap, fixing the previously omitted three bee hooks and three stars in 10-25's Layer 2. It preserves path-over-rail precedence, hidePath, split/automatic hooks, group IDs and float spike angles. Legacy spikesSwitch markers have no source loader and remain ignored. Source tutorial overlays are still not ported.

SpikeHazards now implements linked groups, source widths 202/319/444/559, axis-aligned expanded button hit zones, captured press/release/cancellation, mirrored arrow art, 0.3-second 90-degree rotations and proportional rapid reversals. Collision bands use DX's table-based rotation. Source timeline updates occur before mover transforms. Bee hooks update immediately before each rope's integration, repin only the anchor, and continue moving after cutting. Automatic capture, split ownership and existing interactions stay in the shared physics path. PathMover now follows source float stepping and accumulated circle angles; circular construction uses ordinary MathF-style Cosf/Sinf, distinct from collision's Fm lookup.

AdvancedFrames contains 19 lossless source-registered frames on one reviewed atlas: ObjSpikes 0..7 plus integer-pivot mirrored buttons, ObjBee body/wings/pollen 1..5, and CharSupports 8..9. Original Toolbox/Buzz P1/P2 backgrounds retain their configured seams. Bees use the source (143,196) attachment pivot, 0.77 scale, 60-degree/second directional tilt and three wing frames. Pollen follows authored visible paths with source spacing, size/alpha cycling and draw pass. Roblox ImageLabel alpha blending approximates the source's additive pollen blending; exact additive compositing is not implemented. No skin art was uploaded. All five new assets join startup residency: 283/283 loaded, zero unavailable/failures.

Validation: tools/roblox/run_advanced_reference.ps1 exports 58 real C# routes (50 initial trajectories, four linked/rapid spike sequences, four 600-tick bee runs). All sampled candy and hook-anchor positions match exactly in the final comparison; maximum spike-angle error is 0.00004695 degrees. Body counts, rope topology and bubble ownership agree. All 186 previous Fabric-through-Cosmic differential routes still pass; worst errors are 0.041556, 0.020625 and 0.027532 world pixels for the three suites. Cardboard golden traces remain exact, including existing 1-7/1-15 route checks. Seven new focused checks, ten presentation, ten general-box, eight wheel, eight split and four draw-order checks pass. General checks construct/render all 250 maps and simulate each for 180 ticks. These bounded checks do not prove all puzzles fully solvable; no full Toolbox/Buzz win is claimed this run.

Live Studio review: Toolbox button rotated -70 to 20 degrees, correct linked-arrow art; both boxes entered through normal box scrolling and level buttons; bee/pollen/support/background and spike scenes inspected in screenshots. Fast Gift reeling and Space/pause were exercised with actual virtual mouse/keyboard input. Initial diagnostics caught and fixed a test-only missing sever index and the circular-path trig discrepancy. Premature diagnostic commands during transitions caused tool-only missing-instance errors; normal gameplay scripts did not produce errors.

Evidence: ports/roblox/generated/review/advanced-box-validation.json; advanced-reference.json; ports/roblox/tests/AdvancedChecks.client.luau; AdvancedGoldenChecks.client.luau. Rebuild fixtures using tools/roblox/run_advanced_reference.ps1 then tools/roblox/prepare_advanced_traces.py; sprites with tools/roblox/prepare_advanced_assets.py --emit-luau. All 26 synced scripts read back identically. All 26 diagnostic LocalScripts are disabled for normal handoff; main client remains enabled. Temporary localhost upload server stopped. No publishing or commit. Remaining major mechanics begin with Box 11; audio, tutorial overlays, persistent saves and later boxes remain unfinished.

Final clean startup: main menu visible, empty console, all 283 resident textures loaded, zero unavailable/preload failures. Studio is running normally at the title screen.

## Gift and Cosmic: wheel controls, inverted gravity, 200 maps

Boxes 7 and 8 are now in the playable registry: 200 original authored maps across eight boxes. Import preserves wheel/radius/rail combinations, pumps, spiders, timed and moving stars, bubbles, hazards and all 13 authored gravity-switch records. Source tutorial overlays are still not ported. New registered assets are WheelControl's ObjHook quads 11-14, gravity button faces ObjStarIdle 21-22, Earth 23, CharSupports 6-7, and the four original Gift/Cosmic P1/P2 background sections. Nine lossless quads occupy two reviewed 1024-pixel atlas pages. No Om Nom skin images were uploaded. All six new assets participate in startup residency; 278/278 textures loaded, zero unavailable/failures.

WheelControl follows the original square touch zone, shortest signed angular delta, minimum/maximum reeling increments, maximum geometric rope length and three-point retraction floor. Bungee Roll/RollBack insert/remove actual 105-unit constrained points and preserve DX's integer rest-length rounding, overflow padding and slack adjustment. Refreshed draw points are trimmed after removal. Wheels remain operable before an automatic hook captures candy and cannot accidentally cut their own rope through the wheel's exclusion zone. Base, variable-size inner arm, and independently rotated fixed-size highlight/indicator retain the source's non-inherited child transforms and draw passes.

Gravity is per-level state: one captured press/release toggles it, releasing outside or losing focus/pause cancels. Toggling preserves velocity and Verlet history, reverses rope/candy acceleration and bubble buoyancy, synchronizes all switch faces, and plays the source's 0.3-second Earth half-turn, including rapid-toggle endpoint behavior. Inverted spider departure and candy-break particles follow the same orientation. The Cosmic Earth is anchored to the fitted/tiled P1 origin at (1284,724), not the gameplay camera or viewport center. Gift/Cosmic support art retains its restored canvas while wheel/button/Earth images retain the original trimmed bounds.

The differential tests also uncovered an older rope initialization error: DX repeatedly adds an offset to the previous point; the port multiplied the offset by its index. The two formulas diverge under float rounding on long almost-vertical ropes. DxPhysics now preserves the iterative placement, source float timestep/acceleration/reciprocal-velocity arithmetic, and rope mass changes. Tested Cardboard traces remain exact; the 78 previous Fabric/Foil/Magic comparisons improved to worst error 0.041556 world pixels and the 52 Valentine's/Toy comparisons to 0.020625.

Validation: all 56 new real-C# traces pass (50 initial simulations, two long wheel routes, four gravity routes including a captured bubble). Worst sampled candy-position difference is 0.027532 world pixels; compared live body counts, rope part counts/rest lengths and bubble ownership agree. Eight new focused checks pass for segment rollover/floor, cut exclusion, uncaptured auto wheels, release cancellation, velocity-preserving gravity, mirrored bubble lift/Earth timeline, inverted spider fall, independent wheel transforms and Cosmic background registration. Existing physics, ten presentation, ten general box, four render-order, and eight split/bouncer checks pass. General checks simulate and construct renderers for all 200 maps; they do not prove every complete puzzle route is solvable. Test harnesses now yield between physics cases and wait for the game GUI to avoid startup races/timeouts when suites run together.

Live Studio input: counterclockwise dragging in 7-1 retracted the rope from 11 to 9 points, pulled the candy toward the bottom-mounted wheel, and restored the idle dial after release. In 8-1, a mouse click flipped the switch face, candy/rope orientation and Earth to 180 degrees; subsequent toggles and a left-rope cut collected all three stars. The final attempted drop failed and auto-restarted, so this is not recorded as a completed win. Gift/Cosmic scenes, wheel layers, supports, and both Earth orientations were screenshot-inspected. Normal mouse navigation through Play, the scrolling box selector, Cosmic level selection/8-2, pause/level exit, and Gift level selection/7-1 also passed.

Final handoff: all 20 changed/new Studio scripts match local readback. All 24 diagnostic LocalScripts are disabled. A fresh normal startup and menu/level navigation produced a clean console and 278/278 resident textures. Studio is running on Gift Box's level-selection screen. Temporary localhost upload server stopped; no publishing or commit.

Evidence: ports/roblox/generated/review/wheel-box-validation.json, wheel-reference.json, ports/roblox/tests/WheelChecks.client.luau and WheelGoldenChecks.client.luau. Rebuild with tools/roblox/run_wheel_reference.ps1 and tools/roblox/prepare_wheel_traces.py; regenerate sprite mappings with tools/roblox/prepare_wheel_assets.py --emit-luau. Remaining major mechanics start with Box 9; source tutorials, audio, persistent saves and later boxes remain unfinished.

## Valentine's and Toy: 50 more authored levels

Boxes 5 and 6 now join the playable registry, for 150 maps total. XML import preserves each half's starting point, authored L/R rope ownership, automatic hooks, rail controls, bubbles, pumps, spikes/electrodes, hats, timed/moving stars, and static/moving/rotating bouncers. The legacy hidden03 record in 6-17 has no loader in the current DX source and remains ignored, like the reference. Source tutorial layers are still not ported.

Split candy follows GameScene.CreateSplitHalfBody, SplitCandyState, CandyBodyEligibility, and GameScene.Update: independent physical points and bubbles; prior-frame visual contact starts the closing constraints; 200-unit/second merge; average Verlet velocity and integer rope rest-length transfer; left-then-right ordinary bubble inheritance; no half eating or hat transport; and independent hazard/offscreen removal. The camera tracks the first surviving primary body, as CameraFocusPoint does. Both halves can collect stars and their attached bubbles can be popped independently. Merge FX uses original ObjCandyFx quads 11-15.

Bouncers use the original two widths, both collision faces, desktop swept collision, normal-motion reset and minimum/speed-scaled impulse. An initial comparison exposed two important fidelity issues: DX's 1024-entry float sine/cosine table differs from continuous trig, and buoyancy is applied AFTER bouncers and hazards. DxMath ports that lookup/float arithmetic; DxPhysics now preserves the original constraint multiplication order. The old Cardboard golden fixtures now match exactly in the tested samples. Rope-driven candy rotation is also implemented: one taut rope steers each body, free spin decays, halves rotate their sole sprite, and whole candy rotates only main quad 1 while its shadow/highlight stay fixed.

Assets: 121 source-registered, lossless quads across five reviewed atlas pages: two halves for each of 52 candy choices, both five-frame bouncers, five merge frames, and supports 5/6. Original per-quad trim/canvas metadata is retained; un-restored bouncer/merge frames use the source's trimmed geometry. Four original background sections were also uploaded. prepare_split_assets.py verifies each crop and regenerates SplitFrames from numeric asset receipts. No Om Nom skin assets were uploaded. All nine new images are included in startup residency.

Validation: real C# scene export supplies 52 new trajectory traces (50 starting simulations plus scripted merge and rebound routes). Every sampled body position is within one source-world pixel; worst error 0.981464 in 5-10, merge route 0.016970, bounce route 0.0. All compared body counts/split states and bubble ownership agree. All 78 previous box traces, existing Cardboard physics checks (including 1-7 and 1-15), ten presentation checks, four rendering-order checks, ten general box checks, and eight new split/bouncer checks passed. General box checks now simulate and construct renderers for all 150 maps. These are bounded tests, not claims that every complete puzzle route was solved.

Live Studio mouse input completed Valentine's 5-1 with three stars after merging and cutting the remaining ropes, and Toy 6-1 with three stars after one rebound. In 6-10, clicking the left bubble hid it and produced its pop animation while the right half retained its bubble. Valentine's/Toy scenes, supports, half sprites, and bouncers were screenshot-inspected. Eight focused checks cover all 52 half-frame registrations, independent bubble capture/pop, merge bubble transfer, the two three-star routes, half-only hazard retirement, both bouncer faces, and hat exclusion.

Evidence: ports/roblox/generated/review/split-box-validation.json; split-reference.json; ports/roblox/tests/SplitChecks.client.luau; SplitGoldenChecks.client.luau. Rebuild fixtures with tools/roblox/run_split_reference.ps1 then tools/roblox/prepare_split_traces.py. Final readback matched all 21 changed/new Studio scripts to the local files. All 22 diagnostic LocalScripts are disabled for handoff. Fresh normal startup has a clean console and 272/272 resident textures loaded, zero unavailable/failures. Studio is running at the main menu. Temporary upload server stopped; no publishing or commit. Remaining major mechanics begin with Box 7's wheel-controlled hooks; audio, source tutorial overlays, persistent saves and later boxes remain unfinished.

## Rail seams, electrode registration, draw passes, shortcuts, instant Skip

Traced `Grab.CreateRailVisuals`, `HorizontallyTiledImage.Draw`, `Spikes.InitWithPosXYWidthAndAngleToggled`, and the complete `GameScene.Draw` order. Rail centers now repeat the original 44-pixel quad at native scale instead of stretching it across the whole track. Extruded atlas gutters and one source-pixel overlap hide filtering seams at the cap/tile joins. Caps, track length, authored offsets, and vertical rotation retain their source placement.

Electrode animation frames now share a 507x103 crop at restored offset (163,70), on the original 833x250 canvas. All five frames therefore have identical GUI position/size even when rotated; their original pixels and trim registration are unchanged. `tools/roblox/prepare_box_alignment.py` verifies that restoring each repacked frame produces exactly the original source canvas. One reviewed geometric-object-only atlas was uploaded as 78280721300167; no skins were uploaded. The base generator preserves this extra-page mapping on regeneration.

`GameplayLayers` defines source draw passes for all implemented object families. It changes sibling world-root holders, so nested art cannot override the intended ordering: cushions/hazards/hats before hook backings and ropes, stars before candy, attached bubble/spider/late effects afterward. Candy is now above hats, cushions, rails, and hook art. HUD/pause remain in their separate screen-space parent branches.

Keyboard: R invokes the existing white-fade restart (also from pause); P or Pause toggles the in-game pause. Text-box focus, processed input, menu state, and active transitions are respected. Roblox's `GuiService.MenuOpened` pauses gameplay under its reserved Escape menu; closing that menu leaves the game paused until Continue/P. Studio VirtualInput refuses reserved Escape, so the physical Esc path still needs user confirmation. Reference: https://create.roblox.com/docs/reference/engine/classes/GuiService#MenuOpened.

Skip Level no longer uses any fade, loader, or closing/opening box panels. The next level is built visible-but-occluded behind the existing scene, its crops warmed, then swapped atomically. This is important: a hidden staging frame left some crops unready despite preloading. Last-level Skip similarly returns directly to the level-selection page. Ordinary entry, results, failure, restart, and level-exit transitions are otherwise unchanged.

Validation: four new rendering checks passed (all Foil rail joins including vertical tracks, identical electrode frame geometry, source world draw ordering, attached-bubble/spider ordering); all ten box checks passed including 100 map simulations and 100 renderer constructions. Existing Cardboard physics and all ten presentation checks passed. Live inputs verified P pause/resume, R restart including from pause, 3-1 to 3-2 Skip, final-level 4-25 Skip to level selection, and a three-star Magic 4-1 win with one teleport. Six observed first-visible frames after Skip had no unloaded visible images and a drawable Om Nom; no fade/box/loader overlays were created. R created only the expected white fade. Off/on electrode and rail screenshots inspected. Resident cache: 263/263 loaded, zero unavailable/failures.

Evidence: `ports/roblox/generated/review/render-order-input-validation.json`, `ports/roblox/tests/RenderOrderChecks.client.luau`. Physical Escape and cold published-client behavior are not claimed as tested. No gameplay physics constants changed; no publishing or commit.

Final repeat also checks every rail middle crop remains at native source scale including its sampling gutter. All four checks passed again. All ten changed/new Studio sources read back identically to local mirrors; all 20 diagnostic LocalScripts are disabled for handoff. The temporary local image server was stopped.

## Fabric, Foil, and Magic: 75 additional authored levels

Boxes 2-4 are now playable from their actual XML, making 100 imported levels including Cardboard. `LevelData` resolves per-pack modules; level entry, replay, next, skip, HUD numbering, and background selection use the selected pack. Boxes 5-17 remain explicitly unsupported gameplay, not mislabeled reconstructions. `tools/roblox/audit_box_objects.py` records the remaining object inventory.

`BoxMechanics` ports air-cushion press/drag cancellation and distance-based desktop impulses, clamped horizontal/vertical hook rails, spider activation/walking/theft/cut-fall lifecycles, and paired magic hats with source catch geometry, direction gate, rope/bubble retirement, 0.1-second transit, exit velocity, and 0.8-second cooldown. `DxPhysics` runs teleport dispatch before integration and pump/hat interactions before barriers. Timed stars use source countdown/expiry timing and a radial mask of the actual full/empty ring quads. Electrodes use the authored initial delay and on/off cycle. `PathMover` supports source circular polygons and the special 4-25 initial-angle reset. The importer now preserves unscaled gameDesign mapOffsetX/Y and source integer coordinate parsing; omitting those displaced several Foil/Magic layouts by 16-120 world units.

Artwork comes from C# use sites and atlas metadata: LoadPumps, Grab/CreateRailVisuals/SetSpider, SockArt/LoadSock, Spikes, Star and PackConfig. `BoxFrames` contains 41 exact source crops on three new pages plus six original background/seam images. All nine assets were visually reviewed before upload; atlas pixels and registration were checked against the originals. Om Nom uses the correct per-box support quad. Hat rendering includes GameScene.Draw's temporary -85 Y offset as well as BaseElement's integer half-extents and 0.7 scale; group 0 is red and group 1 green. Spider frame 4 holds through the startup pause; the final walking key remains visible on a loop-wrap tick. Original Pirate art was not uploaded.

Validation completed so far in this batch:

- Ten new checks passed: all 100 maps remain finite over 180 physics ticks; all 100 renderers construct/update; pump input/force, rail clamps/pinned anchors, electrode cycling/collision, circle motion, hat transit/velocity, spider startup/cut, spider theft/timed-star expiry, and hat-color/bubble-retirement cases.
- Real C# scene exports cover 75 initial trajectories and three 240-tick controlled pump/rail/hat sequences. All 78 comparisons stayed below 1 source pixel; maximum 0.770612 world units on Fabric 2-1's early settling (about 0.34 pixels at a 1122px canvas). Controlled rail maximum 0.010933; teleport maximum 0.029025. Sampled spider quads match exactly. These are bounded traces, not proof of long-run bit-identical physics.
- All 21 authored moving objects were compared against CTRMover over 600 ticks. Maximum position error 0.001147 world units; angle error 0.00418 degrees; no destination-index mismatches, including 4-25.
- Existing Cardboard physics suite passed, including all 25 maps, the three-star 1-7 route and 1-15 bubble route. All ten existing presentation checks passed.
- Actual Studio input exercised normal Fabric menu entry, pump clicks, Fabric 2-9's three-star spider/automatic-hook route, Foil 3-1 rail dragging/bubble capture/two-star completion, Magic 4-1 rope cutting/teleport/three-star completion, and failure/automatic white-fade retry. Screenshots inspected Fabric, Foil and Magic backgrounds, rails, pumps, spider travel, timed rings, active/inactive electrodes, and moving red/green hats on 4-25. Not every new level has been solved or three-starred interactively.

Evidence: `ports/roblox/generated/review/boxes-reference.json`, `boxes-golden-comparison.json`, `box-movers-reference.json`, `box-movers-comparison.json`. Reproducible source export: `tools/roblox/run_box_reference.ps1`, then `tools/roblox/prepare_box_traces.py`; Luau checks are in `ports/roblox/tests/DxBoxChecks.client.luau`, `BoxGoldenChecks.client.luau`, and `BoxMoverChecks.client.luau`. The C# export built and both tests passed; disabling analyzers shortened the final repeat to 6 seconds. Studio-only DxStudioLevel attribute requests use the same level-entry flow and exist only under IsStudio().

Remaining: later-box object families (split candy, bouncers, wheels, gravity, toggle spikes, bees, discs, ghosts, steam, lanterns, mice, night/light-bulb logic, transporters), source tutorial overlays, audio, persistent saves, and broader full-route interaction testing. No claim that the complete game is ported. No publishing or commit.

Final readback matched all 21 changed/new Studio scripts and local mirrors. Explicit final assertions passed for all 78 candy traces and 21 mover traces. A second real 4-1 three-star win followed by Next entered 4-2 with the Magic background. All 19 diagnostic LocalScripts are disabled for handoff. Fresh normal startup loaded 262/262 resident textures with zero unavailable assets or preload failures; the console is clean. Temporary upload/build helpers are stopped. The final repeat atlas check again verified all 41 source crops and registration.

## Credits logo, cutting controls, affine box titles, and pipe-free Pirate

Credits now use AboutView's complete CutTheRopeDXLogo asset rather than the title-menu MenuLogoNew layer. The source includes its cupcake/crown artwork; the unchanged complete PNG was inspected locally, uploaded as 74852854661204, and verified in the real credits screen. Options now use full-column transparent hitboxes spanning each cutting-mode icon, caption and checkbox. Actual checkbox/column clicks selected both modes. Click mode completed real 1-1 with three stars after navigating away from Options, with Pirate equipped.

Pack titles and badges now share their box's affine squash transform. FontLayout accepts an optional referenceSize and lays those glyphs out in normalized coordinates; ordinary labels retain existing layout. This also avoids per-glyph integer offset rounding during nonuniform squash. Sixteen live Fabric-box samples covered horizontal scale .95..1.04848: title normalized Y stayed 140/660, maximum center-X error was .00003052 pixels, and glyph relative width remained constant. DxMenuRepairChecks verifies nonuniform glyph/label scaling and the complete credits-logo assignment. Both checks passed, alongside all six UiCorrectionsChecks (including the updated PirateSanitized guard).

Pirate was rebuilt through the game's original XML renderer after removing ONLY its named Image layer `pipe` (shared char_animations_smooth quad19) from an isolated XML copy. All 41 remaining Image layers were audited: none reference that quad. Original content XML and shared source art remain untouched. The safe copy, source hashes and audit are under ports/roblox/generated/pirate-safe. The export retained all ten source timelines, 240 frames and original 768x768 registration. Seven packed pages contain 236 unique poses; all seven were visually inspected, including idle, mouth, chew, sad and sleep frames, with no pipe or pipe fragments visible. Pixel reconstruction passed for all 240 frames. Only these new pipe-free pages were uploaded, with every receipt validated as a numeric asset ID. No original Pirate pages/shared sheet were uploaded.

OmNomSkin07 now contains the cleaned atlas and pipeRemoved=true. SkinAvailability still rejects slot7 unless that explicit marker is present, then requires every page to load successfully. Actual skin-selector input reported Available=true, Equipped=true and Selectedomnom=7; the preview and level 1-1 were screenshot-inspected. Real click-to-cut produced State=won, Stars=3, OmNomSkin=7 and the result screen, with no script errors. Roblox moderation decisions cannot be guaranteed by this content review.

Rebuild order: tools/roblox/prepare_pirate_safe.py; tools/roblox/bake_pirate_safe.ps1 (after building Rendering.Skia.Tests if its exporter changed); tools/roblox/prepare_omnom_atlas.py --pirate-safe; visually review all generated pages; upload only those reviewed pages/update their assets.json; tools/roblox/prepare_omnom_atlas.py --pirate-safe --emit-only; tools/roblox/check_omnom_registration.py --pirate-safe. The generic legacy atlas emitter intentionally excludes original slot7, so run the final safe emit again after any generic emit. Never upload the original pipe-bearing atlas as a retry. Asset receipts and review record are in ports/roblox/generated/pirate-safe. No publish or commit.

Final fresh normal startup: 253 resident textures, 253 loaded, zero unavailable/preload failures, empty console. All 16 diagnostic LocalScripts are disabled. Studio is running at the title screen with normal session defaults. The upload server was stopped; the specific compiler helper spawned during this build was confirmed already stopped.

## Session hint, continuous level entry, and source-based pack scrolling

Replaced the two-box rebuild/black-fade navigation with `PackSelection` and a horizontal `PackScroller` port. Source: MenuController.CreatePackSelect/CreatePackElementforContainer/MonsterSlot, PackStripLayout.For, ScrollableContainer, HBox, Track easing, and ctroriginal_packs.json. The fixed 2560x1440 canvas uses a three-slot 1980-unit strip at X290, 660-unit box canvases, -20 spacing, original leading/trailing padding, five-frame release-velocity sampling, directional nearest-slot snapping, bounce resistance, and max(100, distance * 4 * multiplier) settling. Programmatic snaps use multiplier 0.8. Drag activation uses the source five-unit tolerance and rejects discontinuous jumps above 640 units. A drag that returns to its start cannot become a tap; lost window focus releases capture. Off-strip boxes are culled.

All 17 existing boxes can be centered via drag, arrows, wheel, or Left/Right; Enter/Space opens the settled box. An adjacent-box tap centers it before a second tap opens it, and returning from levels preserves selection. The source half-visible hit-test restriction is retained. The original 0.15/0.2/0.25-second squash timeline, per-box hole colors, perfect-pack badge, Mechanical label, clipped side seams and shadow edges are present. The same source Om Nom crop stays screen-locked behind each box's moving 200-unit reveal window, with the box front drawn above it. Actual screenshots show the hole revealing/cropping the character during dragging. Nested Roblox GUI rounding causes less than one physical pixel of measured character drift, not box-following movement. Labels use source 0.75 vertical / 0.7 English-German horizontal font scaling. Right-arrow crops use horizontal-only UV mirroring, verified in Studio, preserving the source highlight rather than rotating it upside down.

One visually reviewed source texture was uploaded: MenuPackUI, 657x622, asset 95640655976550. All quad assignments come from its JSON and C# use sites. No Pirate art was uploaded. This is menu navigation, not an extension of playable mechanics beyond the existing Cardboard levels.

`Session.claimTitleHint()` allows the title hand once per client session, independently of progress reset. The hand waits for its warmed, visible page, performs the source 0.6-second inward / 0.6-second outward gesture once, fades out, and destroys its extra hitbox. Returning to the main menu does not recreate either object. A new client session can show it again; no persistent first-ever-player flag was introduced.

Separated simulation surface readiness from the input transition guard. Warmup still happens under closed panels or an opaque white cover, but opening panels and the white reveal no longer hold playing-state simulation. Original scrolling-map camera-intro gating is unchanged. A live level 1-2 observer recorded 0.473792 seconds of clock advancement across nine opening frames and 0.090222 seconds across four white-fade frames. Normal pause still pauses simulation.

Validation: seven new pure model/session checks and all six existing UI correction checks passed. Actual Studio input and screenshots verified adjacent-tap centering, drag occlusion, release snapping, wheel, keyboard selection/open, pack-3 return position, rapid repeated arrow navigation to pack 17, and title-hint absence on return. Final cache inspection reported 245 resident/loaded textures, zero unavailable textures, and zero preload failures. Console was clean after repairing a truncated command-output sync; the complete client was read back and matched to the local source. Evidence and exact readings: `ports/roblox/generated/review/pack-selection-validation.json`. Physical touch and cold published-client behavior are not verified.

## UI correction, language fonts, and non-Pirate skin restoration

Source references: GameScene.Show/PlaceLevelLabel, Resources.FontConfig, LoadingView.Draw, GameScene.Update's ObjStarDisappear sequence, and AboutView.CreateCenteredTextBlock. The temporary HUD label now uses original bottom-left anchoring (40 X, 5 bottom inset; 20 for Chinese), the number below the 0.7-scale Level label, and source normalized font metrics instead of a fitted 240x145 panel. Screenshot inspected with its alpha temporarily forced visible; DebugFreeze and alpha overrides were cleared. Its original timing remains unchanged.

Level selection enters the loading page directly, without transitionTo's black overlay. Cutter and tape roller receive their exact initial top-left coordinates before the page can be revealed; loading animation waits for PageReady. ResultContent's fade-in waits for PageReady and uses 0.2 seconds; Replay/Next/Menu await an explicit 0.2-second fade-out before replacing the result screen. Live observer recorded zero black overlays through entry and win, correct first-visible cutter (454,700) and roller (1084,-100), and intermediate result alpha values in both directions. The one black overlay after the exit loader is the existing return-to-menus transition. Evidence: ports/roblox/generated/review/ui-live-observation.json.

Collection bursts now pre-stage all 13 immutable source crops during level setup, avoiding mutable ImageRect page handoffs during the one-shot effect. Source 0..12/20fps ordering is unchanged. Real 1-1 observations recorded one monotonically increasing sequence per collected star, without returning to an earlier quad. Sampling skipped some quads at the current Studio frame rate; this is not high-frame-rate video proof or a claim that the original artwork has only one brightness pulse. Source contact sheet is review/starburst.png. Bursts use the exact 552-unit restored canvas.

FontLayout replaces byte-by-byte iteration and whole-label native fallback with UTF-8 glyph layout. prepare_locale_fonts.py rasterizes bundled GoodDog, Playpen Sans, Cafe24 Dongdong, M PLUS Rounded 1c, and KN Maiyuan using the exact Resources.FontConfig locale mapping. Accented Latin preserves existing ASCII bearings, advances and outline thickness. Other scripts use source language scale and normalized ascent/descent; Korean's three absent punctuation glyphs fall back individually to GoodDog, not to a Roblox font. Sixteen texture pages cover the bundled locale character sets and are resident before menus. All twelve exported locale dictionaries passed glyph coverage and Unicode label checks. Actual Japanese, Chinese, Korean, Russian and French options screenshots inspected; English restored afterward. These are generated glyph atlases, not native uploaded font files.

Credits use black GoodDog small-font glyphs, metric-based wrapping including long words, and responsive block heights. All six blocks render and actual wheel scrolling works. Credits and all cosmetic tabs retain scrolling but have zero-thickness/empty scrollbar images. Font layout has parent/lifetime guards for deferred AbsoluteSize callbacks during destruction. Credits content is still the existing exported blocks, not a full rewrite of AboutView's links/version metadata.

Visually reviewed every packed page of Pharaoh, Caesar, China, Industrial and Cyborg via local contact sheets. Uploaded their 27 previously missing pages; each receipt validated as a numeric Roblox asset ID. Pirate slot7 was never uploaded and emits empty runtime texture references, preventing requests for its old failed pages. All other 14 XML skins are upload-complete, plus classic slot0. Real clicks equipped all five restored skins; Cyborg completed 1-1 with three stars. Fresh resident cache loaded244/244 textures, zero unavailable. Receipts and contact sheets are in ports/roblox/generated/review. Original Pirate source is content/images/animations/om_nom_pirate.xml:1050, Image name="pipe", quad19 of the shared content/images/char_animations_smooth.png sheet. Original source art/XML was not changed.

The first existing presentation suite passed all10 checks. Running it concurrently with the new full-locale suite later exceeded Studio's script execution budget; do not count that run as a pass. The new UI suite passed all6 checks in isolation with no console errors: locale glyphs, long credits wrapping, Pirate exclusion/completeness of other skins, HUD registration, result fades and deferred destroyed-label callbacks. A broad offline all-skin pixel re-audit was interrupted after stalling without output; prior pixel-registration results are not newly reverified by this batch. Upload helper PID15312 is confirmed stopped. All14 diagnostic LocalScripts are disabled for normal play. Final fresh startup loaded244/244, zero failures and no script errors. No commit or publish.

## Unlocked connected ropes, full Cardboard map set, and source mover comparisons

User completed Roblox ID verification. The complete CreateEditableImage + WritePixelsBuffer probe now passes. ConnectedRaster is active in gameplay, replacing all independently transformed rope segment Frames with one editable surface. Inspected screenshots of1-7,1-15,1-17,1-22,1-24 and1-25 show joined curves aligned with their hooks/candy and the scrolling camera. Actual swipes in1-7 and1-15 cut ropes;1-15 captured its bubble and subsequent auto-hooks while scrolling. This is screenshot/interaction evidence, not high-frame-rate video proof of every formerly reported tearing artifact. The documented GUI fallback remains for unavailable APIs or allocation failure. Successful construction now clears stale RopeRendererError.

DxLiveRopeChecks passed actual pixel writes on five maps, camera counter-transform alignment, complete cut-fragment pixel clearing and repeated image disposal.225 submitted frames averaged4.680ms total draw,0.466ms resolve and0.752ms upload, P95 8.083ms/max10.534ms. A subsequent240-draw sample in real1-22 averaged5.215ms/P95 8.305ms. These are rope CPU/submission measurements, not whole-game frame times. No new images or other assets were uploaded. AtlasSprite also accepts a selective immutable-pose warm-up list; classic Blink now stages only source quads41/42 instead of another84 body poses. Classic/XML handoff checks passed127/86 distinct poses with exactly one loaded visible body pose, unchanged geometry, and stale-pending cancellation. All16 idle cadence cases and rope palette/path checks passed.

Imported authored1-17 through1-25, bringing the playable map subset to all25 Cardboard levels. Other boxes remain unsupported.1-20's moveLength=0 is correctly static per GrabAxisResolver; timeout=0 is untimed per Star. PathMover ports CTRMover/Mover polyline traversal: authored offsets*3, integer-truncated speed*3.3, leftover time across corners/loop wrap, duplicate points, pause/reverse and rotation. Moving spike positions AND collision edges advance on the fixed.016 step;1-24's moving star uses its current mover position for both drawing and collection. Motion keeps advancing during outcome presentation. Circular movers, toggled/electric spikes, timed stars and other unsupported objects remain rejected. Existing tutorial nodes remain unrendered; this is not a complete port of every Cardboard presentation detail.

New optional C# RobloxMoverTraceTests exports the actual original mover and scene.129 mover samples over600 ticks (both1-22 spikes and1-24 star, including reversals) matched Luau with zero observed Vector2 error.81 candy samples across new levels17..25 pass the existing2-design-unit tolerance. Max error is1.924540877 in1-19; other new maps range.0012..0805. These are initial120-tick trajectories, not solved three-star routes for all nine maps. Fixtures live in desktop-movers.json, desktop-cardboard.json and generated DesktopMoverTraces/DesktopCardboardTraces. Both C# export tests passed; build emitted style/analyzer warnings, no errors. Production C# game code was not modified.

The broader source comparison exposed an older auto-hook scheduling error: Luau attached a new rope before that hook's simulation pass, but GameScene.Update snapshots/updates the existing rope and only then attaches. Moved capture after the existing-rope pass, with radius fading beginning on the following update.1-19's former5.614-unit mismatch at tick75 is eliminated as a tolerance failure. Existing1-7 and1-15 three-star route checks, all25-map stability, all12 physics checks, all10 presentation checks,52 fragment groups and camera goldens pass. Original trajectory max error remains.02583655 and camera max.00145. Moving-star collection test initially left active rope constraints while manually marking the candy retired; corrected the fixture to call retireCandy(), then the moving-position collection and spike geometry checks passed.

Actual menu clicks entered1-17, then pause/Skip level traversed every level through1-25 with State=playing, ConnectedRaster and no renderer error.1-22 spike positions and1-24 star motion were inspected live. A fresh Play run with all13 Dx scripts disabled completed1-1 with a real swipe and three stars. A self-disconnecting360-render-callback observer spanning the win recorded zero missing/overlapping visible Om Nom body poses; this remains instance-level evidence, not video. Result screenshot inspected. The same three previously unavailable Pirate pages persist (201/204 textures loaded); no moderated asset was retried. All13 Dx diagnostic scripts are disabled for handoff. No publish/commit or asset upload; export/build jobs completed and no task HTTP/helper server was started.

## Om Nom handoff warm-up, connected-rope candidate and static spikes

User confirmed1-15 fully works, then reported a one-frame Om Nom disappearance between animations and rope-only tearing. AtlasSprite previously lazily created each immutable pose and accepted ContentProvider Success as readiness. That did not prove the new GUI instance was drawable. Fixed-frame sprites now allocate their registered poses during construction, stage them visibly at.999 transparency, require the actual ImageLabel.IsLoaded flag over two render callbacks, and retain the prior drawable pose until handoff. Animation changes no longer allocate cold geometry. Returning to the current pose cancels stale pending requests without canceling other warm-up work. Parent destruction disconnects the pump. Atlas-frame geometry remains immutable; original animation schedules were not retimed.

DxRenderChecks passed classic and Original Flash XML transition sequences through opening, chewing, idle, sad, greeting, excited, closing and both variations. Final run observed121 classic/86 XML drawable poses, exactly one loaded body pose at every sampled post-startup handoff, no new pose instances during transitions, and correct delayed/stale request behavior. Earlier classic runs observed127 poses; this count is not a fixed expected total. The first new test used a disabled ScreenGui and exposed that global asset Success was insufficient; the production gate now requires actual label readiness and the test uses an enabled surface behind the game. This is rendered-instance evidence, not a60fps pixel/video proof that every reported disappearance is eliminated. User motion confirmation remains useful.

RopeRasterizer/RopeRenderer are an installed candidate replacement, NOT yet visually validated or active. They port Bungee.DrawAntialiasedLineContinued's shared edge endpoints, ten-vertex strips,1-unit alpha edges, inner bright band,1.02 opaque extension, existing stripe/palette/stretch/cut state and custom outlines. All ropes are composed into one1024x576 EditableImage using2x supersampling and sparse resolve tiles, then submitted in one WritePixelsBuffer call. This replaces independently rounded/rotated GUI strips when permitted. Additive highlight glow remains unported, and finite-resolution raster output is not claimed pixel-identical to DX. [Roblox EditableImage documentation](https://create.roblox.com/docs/reference/engine/classes/EditableImage) documents the1024 maximum dimensions, display update limitations and permission requirements.

BLOCKER: Studio still rejects WritePixelsBuffer with "EditableImage is not accessible. Go to the Security Tab in Experience Settings to enable this API." Creation alone later succeeded, but the write probe still failed; the earlier commentary saying the API was available was corrected. Asked user to enable Allow Mesh / Image APIs via an asynchronous question, with no reply received yet. No security setting was changed by the assistant. Constructor now tests a pixel write before installation, and runtime rendering errors destroy the failed surface and restore the existing GUI fallback instead of trapping loading. Thus the user's rope tearing is not claimed fixed yet. Once permission is enabled, test actual renderer output, geometry, frame times, cut fade and scrolling before declaring success.

Pure raster checks passed shared half-alpha triangle edges without cracks or double blending, clipping/degenerate triangles, and stale-pixel clearing. Initial interpreted six-rope CPU prototype was too slow (~139ms max); native compilation, typed numeric hot paths, scanline spans, sparse resolve and removing per-scanline/per-triangle closures reduced the final isolated30-frame benchmark to5.226ms mean/12.913ms max, with.754ms mean resolve. These numbers exclude the blocked GPU upload and are not whole-game FPS measurements. Renderer CPU geometry is tested; displayed output remains blocked by the permission above.

Added original1-16 and its two static spikes. Importer now emits spike position, authored width index and angle, preserving unsupported moving/rotatable/electro rejection. SpikeHazards ports the original width table212/333/453/566, +/-5 collision band,15-unit candy box and previous-to-current sweep. It preserves CTRMathHelper.LineInLine's original collinear behavior; one newly added boundary test had to move off that collinear case to test box endpoints correctly. Hazards run on the fixed physics step, retire the candy/attached ropes, pop an attached bubble, spawn five source fragments, defer the sad state by.3s and use the existing white loss retry after the additional1s. CandyBreak uses source speed150+/-70, angle-90+/-50, gravity500, rotation+/-600, radial/tangential variance1 and3s fragment life. Particle motion uses fixed.016 steps. RNG differs from DX's generator and original audio remains absent.

The exact Spikes.GetSpikeTextureAndQuad static quads8..11 and CandyBreak.InitParticle quads3..7 for all52 CandySkinHelper resources were packed into two new pages.264 frames passed alpha, premultiplied RGB and registration checks. Static spike bars and particle quads intentionally use trimmed dimensions, matching their respective source draw paths. Uploaded IDs93396030775154 and124282714490647 are in ports/roblox/generated/hazards-atlas/assets.json; each response was validated as an asset ID. These were new source spike/candy fragments, not reuploads of rejected Om Nom pages. Upload helper PID2892 was stopped immediately. No publish/commit, production C# edit or lingering helper process from this batch.

Validation:7 original C# BarrierCollisionTests passed; Luau hazard checks passed source/swept/rotated/boundary cases,30/60Hz retirement, five-fragment/loss lifecycle, and all52 fragment groups. All12 physics checks passed including16 map stability,1-7/1-15 routes and desktop trajectory max error.02583655. All10 presentation checks passed. Camera golden comparisons and all16 transforms passed with unchanged max error.00145. Actual clicks opened1-16; first swipe collected one star, next swipe hit spike2 at(1304.7782,672.0865), retired all attached ropes and displayed loaded source fragments. The one-shot screenshot freeze was released. This is collision/failure QA, not a completed three-star1-16 solution. Fresh asset cache loaded201/204 IDs; the same three prior Pirate failures remain and availability guards are unchanged. Studio is left in Edit with all11 Dx diagnostics disabled so the user can change the API setting.

## Scrolling camera, original 1-15 and cardboard background continuation

Imported the original 1-15 XML, bringing the playable first-box subset to15 levels. The importer now preserves authored map dimensions and permits tall maps while still rejecting unsupported gameplay objects, moving hooks and timed stars. This is not a claim that every box/level is playable. GameplayCamera ports GameScene.StartCamera, UpdateCameraTracking, GameplayCamera.Anchor, Camera2D and LayoutMath.FitCamera into the fixed2560x1440 design canvas. The opening pan starts at the opposite end, accelerates/decelerates with the original constants, can be accelerated by a press, hands input back at the original distance, and switches to proportional candy tracking. Camera updates precede candy simulation. Physics, score time, idle presentation and the transient HUD label stay frozen during the preview. The fixed-step accumulator preserves30/60Hz agreement. Loss bounds now use the authored map height plus the original400-unit desktop margin instead of prematurely failing below a single screen.

Gameplay rendering scrolls/scales independently of the fixed HUD. Pointer positions are measured on the stationary board, transformed into world coordinates for rope cuts/bubble taps, and retained in screen coordinates for finger traces. LevelBackground repeats the actual cardboard P1 and composites its original P2 seam at1120 with1440-unit repeat cadence, following BackgroundTiling/GameScene.Draw/PackConfig. Returning to a short map hides the extra seam pieces. The only new upload was unmodified content/images/backgrounds/bgr_01_p2.png, ID76373038554923; its receipt and copy are under ports/roblox/generated/backgrounds. The temporary localhost upload helper was stopped immediately afterward. No rejected skin art was reuploaded, no game publish or commit was performed, and production C# game code was not changed.

Reference evidence: optional RobloxCameraTraceTests boots the real headless DX1-15 scene and exports normal/accelerated opening pans, candy positions, camera positions/speed/type and input handoff across76 samples. The C# test passed on both runs; rebuilding after formatting emitted style/analyzer warnings but no errors. The fresh second export regenerated an identical Luau fixture. DxCameraChecks passed with maximum numerical error0.00145000000248, plus15 map coordinate inverses, pan freeze/input gating, authored loss bounds,30/60Hz agreement, background adjacency and tall-to-short seam cleanup. All12 physics checks passed, including the existing1-7 route and a new deterministic1-15 bubble route through all six hooks, three stars and the mouth. Existing desktop trajectory maximum error remains0.02583655. All10 presentation checks passed after updating stale test expectations for the corrected lower loss bound and source-localized final-score label.

Interactive Studio evidence: actual clicks opened1-15; a mid-pan screenshot/probe atCameraY671.8848 showed loaded seam art, fixed HUD and candy/score/presentation still at their initial values. The temporary one-shot freeze was cleared. Real mouse swipes cut ropes1,2,3,5,4 as the bubble rose, captured all five auto-hooks, collected3 stars and produced a won result; no direct physics mutation was used for this route. Result screenshot was inspected. Replay and the restart button reset the tall map successfully. All9 Dx diagnostic LocalScripts are disabled for handoff. A fresh normal play run loaded199/202 resident texture IDs; the same three previously unavailable Pirate pages remain failures, not individually confirmed moderation decisions. The new seam is loaded; existing unavailable-skin guards remain. Tutorials, other boxes/background styles, unsupported level objects, original audio and persistent settings remain unfinished.

## Restart recovery, animation verification, idle cadence and rope rendering

Studio e79c0960-fa73-4dea-9a98-3aebfbf261c0 is the same place89236161053799. MCP recovered after a slow startup and supported actual clicks, screenshots and RenderStepped probes. This supersedes the blocked motion-validation note below. No asset upload/reupload, external server, publish or commit was performed.

Fixed-geometry Om Nom rendering was verified live: classic preview180 frames/31 distinct drawable poses, Halloween preview180/44, and Halloween gameplay240/60. All probes recorded zero blank frames, zero overlapping visible body frames and zero mutations of cached crop/position/size. After the idle changes, a fresh diagnostic-free Play run passed another Halloween180-frame/45-pose probe with the same zero faults. This measures instance/readiness continuity, not a pixel-by-pixel video comparison of every skin. The local registration checker also completed a fresh run: all4278 exported frames reconstructed at their original positions, including premultiplied RGB comparisons. No source atlas placement drift was detected by that check.

XmlTargetAnimator now advances idle counters from exported root action keyframe1, matching FlashXmlTargetAnimationBackend, FlashXmlImporter.ParseActionTrack and GameScene.Show. All15 current XML skins have root drivers:14 tick at1.3/.6 seconds and Prehistoric at1.333333/.6 seconds, rather than the former universal one-second tick. prepare_omnom_atlas.py derives first-event/period data from each registered animationXml, checks the baked duration, and retains synthetic one-second fallback for XML without a root driver. Explicit timeline changes reset cadence, ordinary idle wraps retain it, chewing does not advance it, variants start at the event boundary, and followups update the Animation attribute. Skins whose manifest has no idleVariants keep their original loop. No new art was generated or uploaded; generated data still preserves disabled placeholders for the31 failed prior uploads.

RopeDrawing and LevelRuntime now follow RopeColorHelper/Bungee's full-bright custom palettes, original default dark ramp, stretch threshold105+7, stretched brown endpoint palette/red shade boost, optional highlight colors, force-white pending-cut flash,1.95-second cut alpha, and custom-skin black outline(.4 alpha,14-unit width around the10-unit core). No new input highlight behavior was invented. Roblox straight-alpha colors are not multiplied by opacity a second time. Cut path arrays are retained, fragments under3 points are omitted as in DrawBungee, and the Bezier cache now trims old control/sample entries when candy retirement shortens a rope. GUI strips still approximate DX's antialiased joined triangles; this is not an exact rasterizer port. Physics behavior was not changed.

Studio validation: DxRopeVisualChecks passed shrinking/growing curves, retained split buffers, all9 palettes, stretch/highlight/flash/fade. All11 physics checks passed, including the1-7 three-star route and desktop trajectory max error0.02583655 design pixels. DxIdleCadenceChecks passed15 source skin schedules, interrupted/re-entered idle, chewing, optional variants/followups,30/60Hz agreement and synthetic fallback. Its initial failure came from a test assuming every skin has random variants; corrected against the manifest rather than adding nonexistent animations. Actual clicks equipped Halloween2/teal rope2, opened1-1 and performed a swipe: won,3 stars, chewing timeline7. Gameplay screenshot confirmed the teal outline, correct loaded stars/candy/hook/background, and Halloween artwork.

All8 Dx diagnostic LocalScripts are disabled in Edit for handoff. Fresh normal play loaded198/201 distinct resident IDs with3 failures. Those failures are not individually confirmed moderation removals; existing unavailable-skin guards remain. Do not claim all skins are available. The reconstruction process finished normally and no helper process remains from this batch. Remaining skin limits below still apply except the now-corrected idle cadence:20Hz baked source sampling, separate overlays/sounds, intro/outro and native XML rendering remain unported.

## Om Nom fixed-geometry rendering candidate

User reports severe animation flashing/shifting. Classic atlas audit passed255 original frames and registration records. Sampled Halloween idle bounds remain centered in exported frames (bottom370 throughout26 frames), ruling out gross drift in that sampled source loop. This does NOT establish the exact cause of all reported flicker.

AtlasSprite now has an opt-in fixedFrames path, enabled only by TargetAnimator and XmlTargetAnimator (including classic blink). It caches a separate ImageLabel per encountered animation frame, assigning image rectangle, size and position before parenting and never mutating that geometry during playback. Other sprites retain their existing renderer. Pending loads keep the old drawable frame; a RenderStepped retry handles static previews as well as moving animations. Returning to the current frame cancels pending stale draws; destroying the sprite parent disconnects the retry. Cache size is bounded by frames encountered in that sprite's animations.

Initial check exposed a readiness deadlock: a transparent newly created label can keep IsLoaded=false even when its resident image is cached. Corrected the gate to also accept ContentProvider Success. That correction and stale-request cancellation were applied to Studio AtlasSprite with2 successful edits. All3 changed modules are synced. The earlier screenshot showing blank previews was BEFORE this correction, not a passing result.

Final motion validation remains BLOCKED: repeated MCP Client/screenshot calls reported Target not reachable; after temporary recovery the subsequent Start Play request hung while Client execution reported Edit mode. Do not claim this candidate fixed flickering until a fresh Play run is checked. No asset uploads/reuploads, no publish/commit, no external server launched. Existing availability guards and moderation limitations remain. Added tools/roblox/check_omnom_registration.py for local reconstruction checks.

Local reconstruction job completed: all4278 baked frames passed canvas-position/alpha reconstruction checks against packed metadata. Premultiplied RGB comparisons were added to the checker while that job was running, so those additional color checks still need a fresh run. The hung Play orchestration cell was terminated; this is not proof that the underlying Studio request was canceled. Last successful client-mode probe reported Studio in Edit. All local verification jobs finished; no helper process remains from this batch.

## Upload error handling and resumed skin QA

User reported an Assistant image moderated under tobacco/alcohol/drugs, then restarted Studio. New Studio id53317d22-5194-4e0a-a180-639f4f4965dc, same place. No uploads or reuploads were attempted this batch. Pirate artwork contains a pipe, but the exact moderated asset is NOT identified. Await full filename/asset ID if user provides it. Do not assume every failed fetch was a moderation removal.

IMPORTANT CORRECTION to the previous batch: the upload MCP returned successful outer calls with per-image `Error Upload` strings, including HTTP403 `User is moderated`. The previous loop counted those as successes. 31 of102 page mappings were errors, not asset IDs, affecting skins7,8,9,13,14,15. Original upload receipts remain in ports/roblox/generated/omnom-atlas/assets.json for diagnosis. Generator now rejects failed/missing IDs by default. Explicit --allow-incomplete emits empty page placeholders with uploadComplete=false, not error strings or replacement uploads. Regenerated all15 skin modules. No Error Upload strings remain in runtime source.

First restored runtime loaded193/201 resident IDs, with8 failures: omnom4_3=134545958065885, omnom4_4=96990152774717, omnom5_1=139897719507185, omnom6_0=135765573539475, omnom7_0=134613539354275, omnom7_1=111926505807972, omnom7_2=140452453078106, omnom11_1=136324861789180. These are failed fetches, not confirmed moderation decisions. Later diagnostic run had57 unavailable IDs, including preexisting gameplay textures and Roblox avatar assets; availability varies across runs.

SkinAvailability checks all required page IDs and ContentProvider statuses. Incomplete/unavailable choices retain their source grid position with an Unavailable label, no blank selectable avatar. Their clicks do not change selection. LevelRuntime defensively falls back to classic and records UnavailableOmNomSkin. TextureCache now exposes total UnavailableTextures separately from LastFailures for the latest request batch. Corrected selected-preview initialization (greeting/idle then optional variation) and unsupported animation requests no longer mislabel the current state.

Validation: default generator refused all31 bad responses; explicit incomplete mode emitted safe data. Updated Studio checks passed120 state requests,15 chew/proximity cases and16 runtime choices including fallback. Actual clicks equipped Halloween2, attempted blocked Pirate7 (selection stayed2), entered1-1 and cut rope. State won, Stars3, OmNomSkin2, Animation chewing/Timeline7 confirmed. Screenshots inspected for rendered Halloween menu/gameplay, unavailable labels, and thicker readable font outlines. Some OTHER gameplay textures failed to load (stars/hook/support), so this is not a full visual pass of the level. Diagnostic script disabled after use. No helper processes started, no publish or commit.

Final clean Play run with diagnostics disabled:155/201 resident IDs loaded,46 unavailable. Thus failures persist without the diagnostic script; do not attribute them solely to tests. Left Play running on the Om Nom selector for user inspection. No retry uploads performed. Remaining asset failures block complete visual verification; first investigate the exact moderation notice and account/asset availability.

## Om Nom XML skins and thicker font outlines

All 16 Om Nom choices (classic plus 15 XML skins) are wired into SkinMenu and LevelRuntime. XmlTargetAnimator uses the original manifest's timeline IDs, slow-timeline rates, followups, idle variants and start-with-greeting flag. Gameplay switches mouth/open/close, excited, chewing and sad states; chewing is not interrupted by idle variation scheduling. Selection is session-only. Unselected XML previews follow CandySelectionView's Excited/static exception for Original Flash and 14-frame skip; selected previews animate. The classic backend remains unchanged.

Rather than approximate XML skew/pivots with GUI rotations, optional `src/CutTheRopeDX.Rendering.Skia.Tests/RobloxSkinExportTests.cs` runs the original FlashXmlTargetAnimationBackend through Skia. Set DX_SKIN_EXPORT to ports/roblox/generated/omnom-baked and run the single export test. DX_SKIN_LIMIT defaults15; DX_SKIN_FIRST allows resumable suffix exports using the existing index. The test assembly's bin/Debug/net10.0/content is a directory junction to workspace content, not a separate copy. Never recursively delete through that junction. Headless bootstrap's internal render target must be discarded before capturing the export surface. No production C# renderer was modified.

`tools/roblox/prepare_omnom_atlas.py` crops, deduplicates and packs the original renderer's frames into 102 1024px pages, with original canvas registration retained. `--emit-only` regenerates OmNomFrames and OmNomSkin01..15 from packed metadata and uploaded IDs. 4,278 frames exported. `check_omnom_export.py` passed nonempty and four-edge clipping checks for every frame and generated the inspected contact sheet. Emperor and Inventor had edge clipping in the initial512 capture; skins13..15 were re-exported at768 and runtime sizing follows each canvas. Other skins retain512. Current exporter defaults768 for future exports.

Limits: this is baked rendering at20 source samples/second, half-resolution atlas pixels, not a native continuously interpolated XML renderer. Slow timelines play at12 displayed frames/second with original .6 speed/duration. Idle cadence currently uses the one-second synthetic cadence for all XML skins rather than special root-keyframe cadence. Separate sleep/pirate bubble overlays and unique skin sounds are not ported. Level intro/outro timelines are not exported. Preview positioning reuses the gameplay registration at source1.25/1.73 relative scale, so its small custom-anchor difference still needs visual comparison. These are not pixel-identical animations.

Big GoodDog atlas outline increased2->4 raster pixels while retaining the prior corrected advances. Padding was repacked for both big/small fonts; both new asset IDs and rectangle metadata were synchronized, avoiding stale coordinates.

Studio checks passed: 120 state requests,15 chewing-loop/proximity checks and16 runtime skin selections. Actual clicks opened the Om Nom tab and equipped Halloween slot2; Selectedomnom2 and preview Timeline0 confirmed. DxSkinChecks is disabled in Edit after passing. All modified/generated modules and client were synchronized through MCP. No publish or commit.

LIVE VISUAL QA BLOCKED: this Studio session fails asset fetches for old backgrounds/fonts/animations as well as new atlases, plus Roblox's own avatar assets and HTTP403 CoreGui requests. 201 distinct resident texture IDs, only23 loaded,178 failed. One clean Stop/Play retry produced identical counts. Screenshots show missing backgrounds, labels and Om Noms, so they do not validate in-Studio skin appearance or the new font outline. MCP itself works. Ask user to restart Studio/recover asset loading before further screenshot comparisons. The temporary localhost upload server PID9496 was stopped after uploads; do not kill that PID again. Export/build/packing jobs have finished.

## Trace selector and font spacing batch

All11 trace slots now use FingerTraceFactory order and exact FingerTraceSkinOptions quads0..10, restored to234x234 preview canvases. TraceFrames packs81 original preview/particle/glow frames in2 pages; pixel/registration verification passed. `export_trace_presets.py` extracts the11 emitter configurations directly from NamedTracePresets.cs, including both RED layers. FingerTrace connects drag input to source lifetimes, emission rates, particle quad ranges, speed/variance, gravity/radial acceleration, scale, rotation and expiry. Ribbons use the source Bezier sampling count and colors, approximated with GUI strips. Lightning uses head20..23/body14..19, .06 refresh and112-unit grouping. Additive blending, exact triangle-strip edges, glow pivot and some lightning details remain approximations. Do not call this pixel-identical. Trace choice persists only in the current session.

Om Nom tab now contains the functional animated classic preview at source .75 scale and -10 offset. Custom Om Nom skins still require the layered Flash XML renderer, including skew, per-layer pivots, actions, timing and followups. They remain explicitly unavailable; no static substitute is presented as an equipped skin.

Selector labels grew58->68 design units. Font investigation found current DX uses GoodDog TTF while the port used incompatible old sprite glyphs and image widths as advances. Simply attaching current TTF advances to old glyph artwork produced inconsistent spacing, so the final implementation rerasterizes ASCII from the actual GoodDog font. `prepare_current_fonts.py` exports glyph bearing/padding separately; `export_font_metrics.py` exports hmtx advances at matching100/60 atlas raster sizes. BitmapFont positions using bearings and advances, including spaces, instead of consuming texture widths. Small atlas preserves its156px layout canvas. This is Pillow rasterization, not byte-identical Skia output; non-ASCII still uses native fallback and pair shaping is not ported.

Studio test LocalScript DxTraceChecks passed11 complete trace lifecycles plus font checks (12 checks). Script disabled after use. All130 resident textures loaded,0 failures. Screenshots inspected for trace previews, larger labels, tight current-font spacing, and animated classic Om Nom preview. Real clicks equipped star trace3, entered1-1 and performed a swipe; TraceSkin3 confirmed. Particles had expired by the subsequent tool read, so runtime particle appearance is covered by lifecycle tests, not a captured mid-swipe screenshot. MCP logged its own CoreGUI pointer warning, no game-script error. Servers PID12500 and13488 were stopped after uploads.

## Skin menus and button accuracy batch

The title candy opens SkinSelection instead of a placeholder. CandySelectionView's exact skin background, tab quads4/5, slot quads0/1/2/3, candy previews6..57, and rope previews60..68 are used. The grid uses SkinSelectionLayout's four columns, 271x336 cells, 20 spacing, 1.2 row-height factor, and 10 row gap in the 2560x1440 design canvas. All52 candies and9 rope palettes can be equipped for the client session. Om Nom and trace tabs explicitly show an unfinished notice; their runtime/XML animation backends are not yet ported.

`prepare_animation_atlas.py --skins --emit-luau` generates SkinFrames from current root source. Candy resource paths are parsed from CandySkinHelper and Resources, not inferred from asset names. Three gameplay layers0/1/2 per candy preserve TexturePacker registration; menu-logo candy uses the separate MenuLogoNew selected quad. `verify_animation_atlas.py --skins` passed284 exact frames and registration records across14 pages. Original RopeColorHelper base palettes are wired into existing rope rendering; stretch/highlight palette behavior remains unported. All128 resident textures loaded with0 failures.

Long/short buttons now use original up/down MenuButtons quads, including language selection's swapped2/3 states instead of an added checkmark. Title candy shrinks to95% when pressed, changes appearance with selection, and its tutorial hand stops appearing after selection, matching PREFS_CANDY_WAS_CHANGED for the current session. BitmapFont now compensates for inherited UIScale instead of shrinking glyphs twice in the design canvas.

Studio evidence: screenshots checked skin grid and corrected bitmap tabs; real clicks equipped doughnut(index1)/teal rope(index2), then entered1-1 and verified both visibly plus runtime attributes. Scrolled to the final row and clicked candy51; equipped attribute and title-logo quad51 confirmed; tutorial hand absent after selection. Language button normal/unselected quad3, selected quad2, and held press quad2 confirmed. One automation mouse-up failed because each call needs its own position; retried with explicit target and released successfully. No gameplay script errors observed. Original/default candy restored; final screen left on skin selection. Temporary upload server PID12520 stopped immediately after uploads.

## Settings and locale batch

Menu and level-selection shadows now replay the exact MenuController rotation timeline: 45 to 405 degrees over 75 seconds, linear, centered and scaled 2x. Live Studio measurement was 4.947 degrees in 1.029 seconds, consistent with the source 4.8 degrees/second.

Language selection replaces its placeholder with all twelve LanguageHelper entries in the original order and English display names. `tools/roblox/export_ui_locales.py` reproducibly exports original top-level string values from all twelve content/locales JSON files to LocaleData. Localization resolves source keys and existing English UI strings, with English fallback. Options, navigation, loading, reset, result headings/countdown labels, pack names, totals, pause labels, and the transient level label use the locale. Existing credit blocks are translated only where the complete block matches an original locale string; assembled credits are not fully localized yet. Nested tutorial dictionaries are not exported.

Options, pause, and gameplay now share SessionState sound/music/drag settings instead of duplicate local variables. Settings last for the current client session, not across joins. Original audio playback/assets and persistent preferences are still pending; the sound/music controls store shared flags but are not proof of audible playback. Cosmetic selection remains a placeholder, not a completed four-tab skin port. Language selection currently marks the selected entry with a check instead of the source swapped pressed-button quad. Non-ASCII text uses a native font fallback, not DX's localized font atlases. English result stamps/HUD image lettering are still English.

BitmapFont now hides stale pooled glyphs when falling back to Unicode text and restores its holder on subsequent supported text. Result animations also fade native fallback labels. Studio click checks passed: Options -> Language -> Russian -> Options; Chinese selection and rendered options; sound-off and click-cut selections survived language changes; English/default controls restored; Reset -> No returns without clearing records. Screenshots inspected for Russian and Chinese options. Studio console empty after checks. No new textures were needed for this batch.

## DX comparison batch 5 (supersedes physics and transition notes below)

Source-based corrections: GameScene.Update forces delta=.016 each tick; LoadMetadata multiplies authored ropePhysicsSpeed by1.4 for desktop. Both are now modeled while scheduling fixed60Hz ticks. Candy prevPos is explicitly initialized by the loader; newly created rope points initialize on first integration. Bungee cuts defer physical separation until cutTime<1.95, preserve the detached head tip and weaken only owned rope bodies. Bubble capture uses the source square [-85,85), occurs after candy integration and before that same tick's lift, and is one-shot. Lift remains the source -40/damping14, not a guessed gravity reversal. Pop uses the source120x120 square. Om Nom's desktop mouth is now [-56,52] x [30,32] relative to his center, intersected against the candy's [-54.5,57.5] x [-52,52]. Rope width is the source10 design units.

Numerical evidence: `src/CutTheRopeDX.Tests/Interactions/RobloxPortTraceTests.cs` boots the real headless DX GameScene, exports36 coordinates from1-1,1-6 (including a cut and bubble capture),1-7 and1-10 to `ports/roblox/tests/desktop-trajectories.json`. `tools/roblox/convert_physics_trace.py` produces DesktopTrajectories.luau. Studio golden comparisons passed with maximum error0.025837 native design pixels. The same deterministic1-7 route collects all three stars in both the C# scene and Luau simulation: settle60ticks, cut ropes1/3/4, then35ticks later cut2/6, leaving5. This proves star reachability, not complete player-driven level QA. All11 physics checks passed, including the corrected1-10 auto-hook route (51/30tick delays). The two headless C# tests passed. No production C# gameplay behavior was changed.

`BoxTransition` ports the .5s horizontal cover stretch, mirrored cover/tints, side pieces and first-entry seam flaps. Wins close the box without a black fade; results fade in over the closed box. Replay/Next/Skip reopen without a loading page. Restart button and loss retry use .15s white fade phases. Loading pages are restricted to menu->level entry and level->menu exit. Loading text is localized English `Loading...`; cutter quad9 and tape roller quad8 use LoadingView's actual crop dimensions/coordinates and travel, with progressively clipped exit tape. Current cached-load travel uses a fixed minimum .5s presentation rather than DX's resource-manager progress curve.

`ResultPresentation` adds the source star-bonus count, time countdown, final score, alpha phases, and improved-result stamp (MenuResults quad17, source marker12, 3x-to1x .5s ease-in). Confetti appears only for three stars. Result headings use all four English source strings. CanvasGroup requires Sibling ZIndexBehavior; both screens now set it explicitly. BitmapFont reuses glyph labels and its size connection during score changes instead of accumulating duplicate text or rebuilding texture instances.

`LevelHud` restores HudUi quads1..11 at.05s/frame, pausing on11. The bottom-left level label uses the source .5s wait/.5s fade-in/1s hold/.5s fade-out. `--hud` support in the atlas preparation/verification tools exported19 exact frames on one page; pixel/alpha/registration verification passed. New HUD and English stamp assets bring the resident cache to113 textures; a fresh Studio play loaded113/113, zero failures and no console errors.

Interactive checks: real1-1 swipe collected3stars and reached results; Replay returned to gameplay; restart emitted DxWhiteFade. Real1-6 cut captured the bubble, center-click emitted pop quad18, then reached results. Results Menu exercised the exit tape loader; level selection exercised the entry cutter and opening panels. Cutting all1-2 ropes caused a loss and automatic white-fade restart. Screenshots checked title, gameplay/HUD, bubble and result composition. Physics/search probes are retained disabled. No publish or commit.

Remaining: full frame-by-frame visual comparison of panel pivots/fade overlap and the improved-stamp sequence; DX numeric font atlas and exact label fitting; resource-driven loader progress; more extensive post-cut/long-run golden traces; interactive full1-7 completion. Other existing gaps remain: tutorial arrows/text, audio, persistent saves, scrolling maps1-15+, further gameplay objects and other packs. Do not interpret this batch as a full game port or pixel-perfect validation of every screen.

Final presentation suite: all10 checks passed, including win/loss continuation, mouth lifecycle and exact collision region, original idle cadence, confetti/burst/radius lifecycles, HUD quad timing and label fade, score count and glyph reuse. AtlasSprite now gates only cross-page switches on texture readiness: same-page frame changes neither stall nor repeatedly query the content provider. All Studio diagnostic LocalScripts are disabled for handoff.

## Automatic-hook batch 4 (supersedes map limits below)

Real Cardboard maps1-1 through1-14 are now imported. The importer rejects scrolling maps, moving/spider/wheel/gun hooks, timed stars and unknown gameplay objects instead of silently marking unsupported levels playable.1-15 remains gated because it is960 authored pixels tall and needs the DX scrolling camera;1-16 introduces spikes. Tutorial overlays still are not implemented.

`DxPhysics` now models dormant radius hooks separately from pre-attached ropes. Traced source: AutoRadiusSource, GameScene.Update.TryAutoAttachGrabToBody, Grab.CreateAxisVisuals and PhysicsConstants.CandyGrabPadding. Catch distance is authored radius*3+42; generated length is radius+42 (not the authored length field), plus the existing105 initial constraint. The original shared candy endpoint/velocity is retained. Catch is one-shot; cutting cannot reactivate the hook. Radius alpha falls at1.5/sec after catching. Retired/lost candy cannot start another catch. `HookRadius` draws the original blue dashed circle with the source vertex-count rule. Auto-hook art restores ObjHook quads4/5 exactly into276x276 canvases, with the same presentation scale as static hooks. No guessed asset selection.

`EffectFrames` adds all13 ObjStarDisappear frames on two1024px pages; star collection plays0..12 at20fps at the authored star position, then disposes the effect. AtlasSprite supports explicit additional libraries without changing existing animation atlases. The effect pipeline is `prepare_animation_atlas.py --effects`, then upload/update effects-atlas/assets.json, then `--effects --emit-luau`. `verify_animation_atlas.py --effects` passed all pixel/alpha/registration checks. Four new assets bring resident startup textures to111; Studio loaded111/111 with0 failures.

Validation:14-map stability/shared endpoint checks, automatic catch boundary, radius-derived rope length, one-shot cut behavior, ring fade, retired-candy exclusion,30/60fps agreement, and the existing physics/presentation checks pass. A bounded diagnostic search found a three-hook route on1-10: settle60 ticks, cut static hook4, cut auto-hook1 at68 ticks after its catch, cut auto-hook3 at28 ticks after its catch. This is now the deterministic AuthoredThreeHookRoute regression. It establishes that all three catches are reachable, not a full three-star completion test. Search probe is retained disabled. Real Studio swipes on1-10 triggered the first and second catches with ring fade;1-14 cut/bubble capture collected two stars and attached the carried candy to its auto-hook. Screenshot confirmed the collection burst's registration. New maps1-11..1-13 have not been solved end-to-end.

Test sources: PhysicsChecks.client.luau, PresentationChecks.client.luau, RadiusRouteProbe.client.luau. Studio counterparts are disabled for normal play at handoff. Temporary upload server PID5492 was stopped immediately after uploads; do not kill that PID again. No publishing or commit. Remaining major work: scrolling camera, spikes/moving hooks and further objects, source tutorial overlays, audio, persistent saves, Language/cosmetic screens, full collision fidelity.

## Presentation batch 3 (supersedes animation/loading limits below)

Completion no longer stops the runtime. Presentation time, ropes, uncollected stars, bubble pops and Om Nom keep advancing while score time and collected stars stay fixed. Eaten candy is detached from every rope and retired from physics; its consumption tween is no longer overwritten by draw(). The client dispatches the win/loss outcome once using presentation time (2s win, 1s loss), respecting Pause and reset without stale delayed callbacks.

`TargetAnimator.luau` follows OriginalTargetAnimationBackend.ConfigureTargetTimelines/Transitions and TargetIdleState: 20fps idle0..18, random43..67 and the exact 32-frame68..83 sequence, randomized5..20 idle-cycle countdown, blink every3 idle cycles, proximity200 mouth19..27 with final-frame hold and1s recheck, close28..31 then cheerful20..46 on char_animations2, eat28..31 then looping32..40, sad0..12 on char_animations3. Only idle loop keyframe1 advances idle/blink cadence. Feeding still uses the existing approximate95x100 centered intersection, not the complete DX collision-box system. Night skins/post-eat sleep and sound remain unported.

`AnimationFrames.luau` + `AtlasSprite.luau` replace per-frame image URLs for Om Nom, stars, bubbles and confetti with 255 source frames across14 1024px pages. Frame rects and trimmed-canvas registration are preserved. `tools/roblox/prepare_animation_atlas.py` creates the PNGs/frames.json; after uploading changed pages and updating assets.json, `--emit-luau` emits the module. `tools/verify_animation_atlas.py` passed pixel/alpha/registration comparisons for all255 frames. The temporary upload server was stopped (PID748 was the original process and must not be killed again).

Results stars now use original quad13/14 dimensions and offsets instead of equal bounding boxes: filled230x220, slot212x205, common source center with0.5-unit Y correction. `ResultConfetti.luau` ports BoxOpenClose's70 particles, three nine-frame loops at20fps, randomized spin/fall,0.3s growth and2..5s fade. Particles begin advancing after the page transition.

`TextureCache.luau` retains all107 currently used startup/menu/game textures, including all atlas pages, bitmap fonts, covers and HUD states. Studio proved hidden warmup instances remain IsLoaded=false even after fetch; enabled one-pixel images behind the opaque game keep actual textures drawable. AssetFetchStatus gates atlas-page changes so invisible labels don't falsely block animation. Page rebuilds keep the previous page until preloading and two render frames complete; black fades wait for page readiness. Every visible screen is also warmed via its concrete instances. Cold published-client/network-failure behavior remains unverified; retry failures are surfaced as warnings/attributes, not silently claimed successful. Confetti birth frames can briefly report IsLoaded=false before their zero-size sprites start drawing, despite the resident atlas being loaded; screenshots did not show missing confetti.

Validation: all earlier physics checks and five new PresentationChecks passed (win/loss continuation, exact idle sequence/cadence, mouth lifecycle, confetti lifecycle). Studio checks are retained disabled for normal play. Real1-1 swipe, replay, results/confetti screenshot; win clock advanced2.2s while score remained unchanged; Pause during chewing prevented results until Continue. Real1-2 loss reached sad quad12 with0.668s presentation advance and unchanged score, then retried. Options audio/input toggles rebuilt with no missing visible images. Real1-6 bubble capture preserved identical candy/bubble positions with quad12, then clicking produced pop quad18. A temporary DebugFreeze was used to inspect bubble capture and was cleared afterward. Fresh normal session loaded107/107 textures with0 failures and no console errors. Studio raw mouse coordinates were observed matching InputObject.Position directly in this run; do not hardcode an extra GuiInset correction.

Current scope remains real Cardboard1-1..1-9, limited fixed hooks/ropes/bubbles. Other packs/levels are browseable placeholders; audio, persistent saves, tutorial UI, more objects and full collision fidelity remain unfinished. No commit or publish performed.

## Gameplay batch 2 (supersedes the limits below)

Cardboard 1-1 through 1-9 now load from original XML via `tools/roblox/import_classic_levels.py`. Coordinates use the source 2560x1440 canvas, x centering from authored map width, and mapScale=3. LevelData.supported gates real maps; later cards remain explicit placeholders. Next and Skip advance through these nine maps; stars and best scores are tracked per level for this session.

`DxPhysics.luau` ports the supported PC point/rope mechanics: shared candy endpoint, mass-weighted distance constraints, source initial 105-unit link plus authored length, .02 rope-point mass and candy mass 1, 30 relaxation passes per rope, QCP delta*.016 integration for rope points, delta squared for candy, gravity 784. A fixed 60 Hz accumulator makes 30/60 Hz render updates agree. Bubble lift uses the source per-frame impulse (-velocity/14 plus -40Y), not reversed gravity. Cut ropes separate and fade; their tail constraints are removed when the fade finishes. The cut implementation is simplified relative to DX's inserted cut endpoint and brief white flash.

`LevelRuntime.luau` owns gameplay rendering, stars, bubbles, win/fail states and cut interaction. The client no longer freezes attached candy. RopeDrawing now caches work per point list rather than allocating a fresh table for every Bezier sample. Bubble mapping was corrected using BubbleAnimationFactory and LoadBubbles: free = quad0 + shadow quad1..3, captured loop = quad4..16 at20fps, pop = quad18..29 at20fps. Free/pop original canvas449x446 and carried canvas250x250 are scaled independently.

Checked in Studio: 1-1 settles around y511 versus source rest endpoint510 and completes with three stars; Next enters1-2; cutting one of1-2's ropes leaves two attached; six-rope1-7 renders and remains stable; Skip advances to1-8; actual click captures/pops bubble on1-6 and completes it with three stars. Final carried-bubble screenshot and properties confirm candy/bubble positions are identical. All nine maps pass stability/shared-endpoint/pinned-anchor checks, along with frame-rate independence, 1-1 settle height, independent cuts, and lift/pop physics checks. Those checks are in ports/roblox/tests/PhysicsChecks.client.luau and a disabled Studio LocalScript DxPhysicsChecks. Not every map has been solved end-to-end.

Studio-only `GameLayer.DebugFreeze` pauses simulation for precise inspection. Normal UI input remains active so a frozen bubble can be clicked; reset clears the flag. CandyX/Y, AttachedRopes, Stars, Bubbled, State and Level attributes provide read-only diagnostics. The automated mouse API's raw-coordinate path has a top-bar offset, but Roblox InputObject.Position already handles it: no extra GuiInset subtraction belongs in the client.

Still missing: radius/auto-attach hooks (introduced at1-10), moving hooks, spikes, scrolling maps, tutorial overlays, Om Nom mouth/chew/sad animation, audio, persistent saves, Language and cosmetic selection screens. Star collisions use source-sized axis-aligned boxes; target feeding currently uses an approximate centered box and does not yet reproduce the source mouth state. This is not a complete physics/gameplay port. Old single-rope RopeSimulation remains as an unused reference module. No external helpers were launched in this batch.

## Previous batch

Local Luau mirrors match the Studio scripts at the end of this batch. ReplicatedStorage files live under `game.ReplicatedStorage.CutTheRope.Shared`; the client lives under StarterPlayerScripts. No publish or commit was performed.

Implemented this batch: Reset confirmation; source credit text with scroll and manual-scroll cancellation; source score formula and session records; pause-input lexical scope fix; guarded transitions; generation-guarded win delay and consumption tween; out-of-bounds retry; original ASCII big/small bitmap fonts using two 1024px atlases; instance-based texture preloading; original Bezier sampling and rope shade grouping. The 1-1 rope has four constraints, from source length 90 * mapScale 3 plus the initial 105-unit constraint. Atlas generation preserves glyph pixels and rectangle metrics.

Verification: Studio screenshots and actual button/mouse interactions through Options, Reset/No, Credits/Back, packs, level selection, loading, 1-1, Pause/Continue, results, Replay and return navigation. A swipe while paused did not cut; an unpaused swipe and click-to-cut each completed 1-1 with three stars. A timed result displayed 5837 with elapsed 1.6360844038426877, matching ceil(3000 + max(0, 30 - elapsed) * 100). Latest complete run reported zero preload failures and no console errors. Warmup via raw asset strings had failed; ImageLabel instance requests resolved that in the tested run. Cold published-client loading remains unverified.

Important limits: only 1-1 is playable. Other 17-pack/25-level cards still lead to explicit placeholders. Language and candy selection remain placeholders; music/sound toggles do not yet drive audio. Gameplay is still a limited custom simulation, not a complete DX physics port. Attached candy is frozen until cut. Rope rendering follows the source sampling, but full constraint dynamics, antialiasing, cut-tail motion, eating/sad animation and full results animation remain unported. Credits body still uses the fallback Roblox font. Session records are not persistent saves. Failure retry was added but not reachable through ordinary 1-1 testing. Next on results currently returns to level selection.

Source references: GameMain/LevelResult.cs; GameMain/AboutView.cs and content/locales/en.json; MenuController.CreateReset; GameScene.GameLogic.GameWon/GameLost; Bungee.DrawBungee and RollplacingWithOffset; Framework/Visual/DrawHelper.CalcPathBezier; RopeColorHelper.GetDrawColors; GameScene.Show mapScale=3.

The Windows DX trace `ports/roblox/generated/port-trace-2026-09-16.jsonl` still had only startup events when inspected this turn. No new Windows build was launched. Both temporary Python upload servers were stopped after use; PIDs may be reused and must not be killed again.
